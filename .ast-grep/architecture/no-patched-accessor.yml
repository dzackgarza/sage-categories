id: no-patched-accessor
language: python
severity: error
files:
  - src/sage_categories/**/*.py
ignores:
  - src/sage_categories/kernel/**
message: POL-LEAF-064, D133
rule:
  pattern: '$OBJECT.$NAME = lambda: $BODY'
constraints:
  NAME:
    regex: ^[A-Z]
