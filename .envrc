source "$HOME/.envrc"

# Default to the research-image wrappers created by `just ci-provision-sage`.
# An explicitly configured research Sage executable takes precedence.
export SAGE_BIN="${SAGE_BIN:-/usr/local/sage-env/sage}"
