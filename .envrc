source "$HOME/.envrc"

# Use the installed host Sage unless an executable is explicitly selected.
# This checkout keeps its runtime source compatible with Sage's Python 3.12;
# CI may still provision a separate wrapper through ci-provision-sage.
export SAGE_BIN="${SAGE_BIN:-$HOME/.local/bin/sage}"
