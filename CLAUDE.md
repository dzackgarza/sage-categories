# Claude Code adapter

The repository's agent guidance has one durable owner: `AGENTS.md` (with
progressive disclosure into `.agents/`). Claude Code auto-loads only
`CLAUDE.md`, so this file exists to pull that context in. Do not add
content here — edit `AGENTS.md`.

@AGENTS.md
