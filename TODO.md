# Mathematical execution DAG

The active DAG converges on two milestones, in order:

- **A — Complete kernel and Cat subtree:** the shared foundation supplies the full specified categorical interface, inheritance, initialization, coherence, universal constructions, and static projection through a small mathematical declaration surface.

- **B — Correct, minimal mathematical leaves:** every production leaf uses that foundation through mathematical data, immediate functors, local operations and comparison data, with private backend wiring.
  The shared machinery is blackboxed in actual leaf code and execution.

This file owns the execution order.
[DECISIONS.md](DECISIONS.md) records the architectural direction; [the system ownership map](specs/system.md#ownership-map) and its topic specifications own the full mathematical/public contracts.
The [native-engine remediation plan][plan] is archived and explicitly delegates subsequent framework completion to this DAG. Its engine allocations and preservation obligations remain applicable; its historical completion does not establish either milestone below.

The existing public work belongs under [framework #36](https://github.com/dzackgarza/sage-categories/issues/36), with foundational obligations in #4/#34 and downstream obligations under #38. Existing issue contracts and dependencies remain binding.
A closed remediation slice does not close a broader issue or establish the stronger architectural acceptance requested here.

Start from delivered source and valid consumer evidence, and preserve work already in flight.
The new nodes are open acceptance obligations, not assertions that every listed capability is absent.
Reuse working implementations and still-valid evidence; repair concrete missing behavior at its owner.
Earlier completion and remediation rows are retained below as historical inputs, not competing work selectors.

Read [AGENTS.md](AGENTS.md), the named [contribution policies](CONTRIBUTING.md), and [COMPLAINTS.md](COMPLAINTS.md) before selecting affected work.
The [continuation locator](docs/remediation-handoff.md) supplies plan retrieval when the local vault link is unavailable.

## Active framework-completion DAG

An ID in another node's `Needs` column is a prerequisite; multiple prerequisites are conjunctive.
Among ready active nodes, select the first row unless a later owner instruction selects another ready node.
Finish that node's complete public consumer before changing fronts.

Milestone A includes the smallest real leaf consumers needed to prove its generic capabilities.
Such prerequisite repairs and consumers belong to A; full production-leaf convergence belongs to B. This avoids a cycle in which the kernel is declared complete before any leaf can use it, or leaves must repair missing generic machinery themselves.
A leaf-discovered generic defect reopens its A owner and affected acceptance, without invalidating unrelated evidence.

Every behavioral node carries its exact static consumer and applicable architecture checks.
Integration nodes combine already-exercised capabilities at one committed revision; they never postpone a capability's first working consumer.
Closure covers the complete contracts at the linked owners, including infinite and nonenumerable domains.
Finite examples, green import checks, or a few representative leaves cannot discharge a universal claim.

Before authoring a node, follow its existing owners below through the named public consumer.
Identify the exact contract it fails and extend that owner by the smallest missing behavior.
If the consumer already satisfies the full obligation, retain its implementation and evidence; an open acceptance row is not permission to rewrite it.
Record the demonstrated failure and repair with the existing issue/commit evidence.
A replacement must transfer the complete responsibility and preserve every affected consumer.
Reuse Sage's C3/class construction, SymPy's propositions, native cells and existing engine derivations directly; a wrapper around a second implementation does not count as reuse.

A's enforcement obligation is to implement and exercise general admission, import, typing and mathematical-contract mechanisms through their first real consumers, including invalid nearby declarations and real engine reconstruction.
B's enforcement obligation is to establish conformance of **every** production leaf and helper to those mechanisms and its full mathematical contract.
A does not require B's all-leaf review to have finished.
Every existing mandatory gate remains binding: a failing gate is repaired at its responsible prerequisite, never suppressed, excluded, or recorded as accepted to break this sequencing boundary.

### Milestone A — Complete kernel and Cat subtree

**Consumer:** a mathematical author introduces a category using its defining data and immediate functors, then obtains inherited object, point, and morphism operations, comparisons and universal constructions without orchestrating runtime state.

**Scope:** all responsibilities of `kernel`, `cat`, and `cat_kernel`, their bootstrap, and the private native integrations that execute their contracts.
Completeness is measured against every applicable public declaration and topic contract in the system ownership map, including the corrections in `DECISIONS.md`; it is not restricted to the illustrative consumers named in the rows.
Mathematical algorithms remain delegated to their existing engines.

| ID | Work and complete consumer boundary | Needs |
| --- | --- | --- |
| `core-owned-interfaces` | **Closed 2026-09-19.** The shared declaration/implementation boundary is identity-owned and typed: `Cat` retains actual `DeclaredCategory` and `CategoryFamily` objects rather than semantic string keys; the former `cat/assembly.py` registry surface is removed; mathematical construction/selection families own typed `ChosenConstruction` / `SelectedChoice` values over private kernel identity retention; and native conversion remains at private adapters. Named-category implementation strengthens the retained declaration in place, while later exact-category augmentation preserves earlier category objects, points, morphisms, identity maps, selected functors, and installed role surfaces. Distinct declarations may share display names without becoming one owner. Generated projections expose the same typed interface without `Any` or string dispatch. **Acceptance:** the named-set declaration consumers, exact-derived implementation consumer, declared-construction consumer, exact identity/augmentation `m2` consumer, duplicate-display-name regression, focused static type consumer, generated projection, architecture contracts, and current plan-state pass under the declared Sage/Python runtime. | none |
| `core-functor-cell-calculus` | **Closed 2026-09-19.** The existing owned `Mor`/`Fun` tower, Catlab transformation calculus, homotopy-core cell adapter, and DisCoPy comparisons remain the sole calculus owners. `Fun(C,D)` now distinguishes formal generating 2-cells from executable natural transformations without a second cell representation: a formal generator retains its exact parallel functor boundaries and directed/invertible native classification but has no component rule, while an executable transformation retains its Catlab component assignment. Formalness is preserved through vertical composition, both whiskerings, horizontal composition and formal inversion; executable natural isomorphisms derive executable inverse components when the component inverses exist. **Acceptance:** the nonfinite Catlab functor/transformation consumer passes for object/morphism actions, vertical composition, both whiskerings and horizontal composition; the formal-vs-executable consumer passes for directed/invertible generators, formal inverse/composition/whiskering/horizontal calculus and component rejection, plus executable componentwise inversion; nonidentity interchange, higher `Mor`-tower boundaries, native invertibility classification, nonstrict DisCoPy comparison maps, exact Cat-core Fun/Mor ownership, focused static Mor/Fun consumers, generated projection, architecture contracts and current plan-state all pass at the exact modified revision. | `core-owned-interfaces` |
| `core-selected-transport` | **Closed 2026-09-19.** Selected object and morphism actions retain their exact public images and initialize inherited owners in declared dependency order, once per shared runtime owner; composite selected isofibrations preserve the source-derived state through every target. Point selection now uses the same categorical placement/refinement join as other structure instead of a one-point special case: every selected point functor contributes its generated essential-image inclusion, so sibling point structures coexist and the level shift installs each distinct category/object surface in either declaration order. Parameterized axioms can state the exact category owned by their parameter; `EssentialImage(F)` therefore remains owned by `F.codomain()` even when that codomain is itself a declared subcategory, instead of being misrouted through its ambient. **Acceptance:** `test_runtime_selected_actions` passes for two targets in both selected orders with retained object/morphism images; `test_composite_isofibration_transports_generic_state` passes; the selected-action initialization and once-only image-retention consumers pass; refinement preserves existing descendants; the original single-point level shift passes; the new two-sibling-point consumer passes in both point orders with both category and element surfaces; the existing parameterized-`EssentialImage` consumer remains green; focused static Mor/Fun consumers, generated projection, architecture contracts and current plan-state pass at the exact modified revision. | `core-functor-cell-calculus` |
| `core-universal-calculus` | **Closed 2026-09-20.** Chosen limit/colimit presentations retain their exact diagrams, apexes, oriented legs, mediators, and induced maps independently of apex identity. Universal factor equations are retained lazily from the presentation at the exact requested leg, so infinite shapes require no enumeration and public composites reduce to the retained competing cone/cocone component without leaf-specific mathematics. `Colim_I` now derives its morphism action from the authoritative public initial cocones through `LeftUniversalArrows`; finite category limits reconstruct through component rules; the Catlab/homotopy boundaries retain the exact public functor/transformation endpoints required by the Kan and category-valued consumers; and `Sets` morphism equality preserves the exact identity case before its stronger extensional handlers. Shape-dependent limit creation/lifting still requires explicit supplied lifting data rather than faithfulness alone. **Acceptance:** `test_universal_constructions.sage`, `test_cat_construction_consumers.sage`, `test_intrinsic_universal_presentations.sage`, `test_lifted_limits.sage`, `test_universal_arrow_calculus.sage`, `test_indexed_products.sage`, and `test_sequential_colimits.sage` pass at the exact modified revision, covering distinct presentations sharing an apex, dual cone/cocone data, nonidentity universal factors, explicit creation/lifting, Kan factorization, arbitrary sequential stages, and infinite/nonenumerable indexed products/coproducts while preserving #24/#30/#32 and the existing `universal`, `indexed`, and `kan` contracts. | `core-functor-cell-calculus` |
| `core-properties-refinement` | **Closed 2026-09-20.** The existing SymPy proposition/query layer and generic property/refinement machinery remain the sole owners of axioms, property categories, intersections/inverse images, placement, equality, assumptions and typed queries. The public Cat surface again exports `assume` with the generated static projection, and finite presented shape vertices resolve raw labels intrinsically so generic property pullbacks do not require the Sets leaf during Cat-only execution. Positive property evidence refines the same value; negative and undecided propositions add no placement; exact Hom/functor endpoints remain fixed and mismatched natural-transformation endpoints are rejected at admission. No production leaf imports or invokes the kernel refinement operation. **Acceptance:** `test_m3_properties_queries_refinement.sage`, `test_refinement_descendant.sage`, `test_m5_vertical_witnesses.sage`, the exact-endpoint natural-transformation consumer in `test_m2_cat_core.sage`, `test_composite_isofibration_transport.sage`, and `test_predicate_set_subobjects.sage` pass under the declared Sage/Python runtime; `just generate-stubs` passes with the current property/universal projections. These cover generic retained pullbacks, same-object descendant preservation, exact endpoint rejection, selected-functor property transport and genuinely `Unknown` predicates while preserving #31/#35/#41 and `specs/property-refinement.md` / `specs/undecidable-properties.md` in full. | `core-selected-transport`, `core-universal-calculus` |
| `core-inheritance-coherence` | **Closed 2026-09-20.** The compiler consumes ordinary retained executable invertible natural transformations between the exact composite structure functors represented by competing selected-inheritance paths. The declaration-order path remains the once-only initializer; when one executable comparison exists, its component transports the preferred target image to the alternate public image, while shared runtime owners still initialize once. Missing, merely formal, or non-unique executable comparisons leave the preferred route in place and emit the exact opt-in diagnostic naming both composites, the inherited target, and the required comparison category; an ill-typed executable component fails at its exact target Hom boundary. Declaration order therefore selects precedence rather than asserting coherence. Sage controlled C3 remains the sole runtime linearizer: the comparison key again reflects declared selected-functor precedence, and already-compiled C3 chains are retained as fixed ordering constraints so later property-category construction cannot invalidate cached MROs. **Acceptance:** the complete generated `test_m1_sage_runtime` consumer passes 19/19 at the modified revision, including the nonidentity comparison, unresolved comparison diagnostic, distinct structures sharing a target, once-only selected transport, declared-order, refinement, collision, identity and dynamic-property consumers; both `test_static_projection_normalization.sage` consumers pass; `just generate-stubs`, focused Ruff, import, and diff-integrity checks pass. Coherence data remain ordinary cells in `Fun(C,D)`; the compiler invents neither fillers nor proof/certificate records and requires no per-leaf infinite witness tower. | `core-selected-transport`, `core-properties-refinement` |
| `core-indexed-calculus` | **Closed 2026-09-21.** The existing `IndexedCategories`/`Grothendieck` and `Adjunctions`/`Equivalences` owners remain the sole selected-data calculus. A nonconstant pseudofunctor with nonidentity reindexing and a supplied nonstrict compositor now exercises total-category composition, cartesian lifts and factorization, while a pseudonatural transformation with a nonidentity comparison transports both objects and arrows through the induced Grothendieck functor. The selected fiber equivalence retains its actual unit and counit and exposes the corresponding adjunction. The consumer also exposed a stale Sage `subcategory_class` after dynamic runtime refinement; compiler cache invalidation now clears it with the other derived runtime classes, so the existing duality/equivalence consumer survives the same refinement boundary. **Acceptance:** the complete `test_indexed_constructions.sage` consumer exits successfully under the declared Sage/Python runtime, including nonidentity compositor, cartesian-factor, induced-arrow and selected adjunction/equivalence assertions; the targeted `test_duality_retains_equivalence_data_and_transports_transformations` consumer also passes at the same modified revision. | `core-universal-calculus`, `core-inheritance-coherence` |
| `core-weighted-calculus` | **Closed 2026-09-21 at `f97eb8e4`.** The existing Kan, weighted, profunctor and relation owners remain authoritative. Weighted Hom-end execution now resolves native finite-set indices through owned point equality and canonicalizes equivalent reconstructed finite vertices before reading retained cone/cocone legs. Yoneda can consume a supplied Hom bifunctor, so `j.restricted_yoneda(Hom_C)` is the actual restriction `C -> Fun(A.op(), S)` without requiring finite Hom enumeration. A separating test functor now retains its canonical evaluation natural transformation: the source functor is the nested indexed coproduct of `j(a)` over probe objects and maps `j(a) -> X`, its morphism action is induced through the two coproduct universal properties, and each component is retained in the epimorphism subcategory from the supplied faithfulness hypothesis. Both coproduct indices may be represented infinite/nonenumerable sets. **Acceptance:** the retained runtime batch exits `STATUS=0` for `test_regular_relations.sage`, `test_universal_constructions.sage`, `test_universal_arrow_calculus.sage`, `test_weighted_universal_maps.sage`, and `test_profunctor_composition.sage`; the current-HEAD `test_separating_families.sage` consumer exits `STATUS=0` with `A = Discrete(NN)`, predicate-represented Hom sets, a nonidentity shift, explicit summand injection and evaluation naturality; `just generate-stubs` exits `STATUS=0`; the focused `tests/static/weighted_calculus.py` mypy consumer is green against the generated projection and public reexports; focused Ruff and diff-integrity checks pass. This preserves the full #32/#39–#44/#46 contracts without narrowing them to native finite execution. | `core-universal-calculus`, `core-inheritance-coherence`, `core-indexed-calculus` |
| `core-algebraic-calculus` | **Open.** **Reuse:** `cat/monoidal.py:MonoidalStructures/Actions`, `structured_objects.py:Inserter/Equifier`, `modules.py:Modules`, `bimodules.py:Bimodules`, `base_change.py`, and their engine integrations; begin with `test_monoidal_structure.sage` and retained module/tensor consumers. Extend the demonstrated missing construction or map in this calculus. **Acceptance:** supplied nonstrict tensors, an action with different acting/acted-on categories, internal algebraic objects, modules, bimodules and named derived/forgetful/transport functors obtain operations through their defining diagrams. Algebraic and geometric first consumers use generic additive/tensor/descent and composition without duplicating them. Preserve full #40/#42–#46 contracts and scalar/domain generality. **Retained boundary:** `f325fe12` routes bimodule maps through the commuting-law full-subcategory restriction. `test_bimodule_morphism_owners.sage` rejects individually lawful but noncommuting endpoints and checks nonidentity maps between distinct carriers, both action projections, inverses, identities, and composition; its complete Sage load passes, as does the focused `tests/static/bimodule_morphisms.py` consumer. Independent `r-gate` review remains pending after a rolled-back launch (see `COMPLAINTS.md`); this boundary does not close the node. | `core-universal-calculus`, `core-inheritance-coherence` |
| `core-structured-calculus` | **Open.** Integrate `core-indexed-calculus`, `core-weighted-calculus` and `core-algebraic-calculus` through their existing owners and accepted consumers. This row owns their interoperation, including all specified shared Cat constructions: adjunctions/equivalences, indexed/Grothendieck constructions and lifts, Kan and weighted constructions, ends/coends, profunctors/relations, Yoneda and separating-family constructions, supplied monoidal categories/actions, internal algebraic objects, modules and bimodules, and their named forgetful/transport functors. Generic additive/tensor/descent responsibilities stay at their mathematical owners while native computations retain their private allocation. **Acceptance:** full public construction families carry both actions, defining maps and universal factors, with nonstrict/nonidentity comparisons and a supplied action whose acting and acted-on categories differ. Algebraic and geometric first consumers obtain these operations without reimplementing generic composition, compatible-family algebra or quotient descent. Preserve the broader #32/#39–#44/#46 contracts, not only their historical native slices. | `core-indexed-calculus`, `core-weighted-calculus`, `core-algebraic-calculus` |
| `core-static-and-boundaries` | **Open.** **Reuse and extension boundary:** `cat_kernel/static_projection.py`, `kernel/stub_generator.py`, `tests/static`, Import Linter contracts in `pyproject.toml`, `.ast-grep`, `scripts/rule_coverage.py`, and public category/functor law consumers. Extend these owners rather than introducing a separate declaration schema, source-pattern blacklist or parallel test framework. Complete the integrated static projection and enforce the shared ownership boundaries from the authoritative declarations. Exact constructor parameters, role relationships, Hom endpoints, selected structures, refinement identities and functor variance survive projection. Imports and typed interfaces prevent leaf access to compiler/retention/native internals, including through forwarding helpers. **Acceptance:** `just generate-stubs` and the exact static consumers cover the same integrated operations as runtime; `just architecture` and `just architecture-boundary` enforce exhaustive classification of every source module (new modules cannot go unclassified), protected runtime/native modules with designated allowed importers, and layer/forbidden contracts including indirect paths through helpers. Preserve legitimate imports of immediate mathematical targets. Exercise valid and violating consumers of each contract, including a new unclassified module and an indirect forbidden import; a passing command alone is insufficient. Derive automatically applicable category/functor law and transport/universal/reconstruction obligations from authoritative declarations, so a new applicable leaf inherits them without hand registration in a test inventory. Use existing testing machinery and generated/stateful inputs where they expose order or owner interactions; declaration diagnostics expose invalid ownership/endpoints without `Any`, suppression or string-dispatch escapes. Existing feature-level checks remain required at each preceding node. | `core-structured-calculus` |
| `kernel-cat-complete` | **Open — milestone A.** **Reuse and extension boundary:** the preceding A owners and their complete public consumers; reconcile the specification ownership map and decision coverage below against actual declarations and evidence at this revision. At one committed revision, every kernel/Cat/joint-layer contract in scope is implemented and exercised through its complete public consumer, including the required first leaf consumers. Shared inheritance, initialization, coherence, construction, placement and backend coordination are blackboxed from those authors. Account for every public shared declaration and applicable specification obligation; the named examples are not the coverage limit. **Acceptance:** independent review follows the actual constructors, functors, cells, inherited operations and results, with current runtime/static evidence and applicable architecture gates. Missing generic behavior keeps A open even if all individual historical rows are closed. | `core-owned-interfaces`, `core-functor-cell-calculus`, `core-selected-transport`, `core-universal-calculus`, `core-properties-refinement`, `core-inheritance-coherence`, `core-structured-calculus`, `core-static-and-boundaries` |

### Milestone B — Correct, minimal mathematical leaves

**Consumer:** a mathematician reads or extends any production leaf in the language of that domain.
Its public source states mathematical data, immediate functors, hypotheses, local operations and genuinely additional comparison/lifting data.
Private adapters wire mature engines to owned inputs and outputs.

**Scope:** every production module and helper in `sets`, `order`, `algebra` and `geometry`, including their public exports and private engine boundaries.
Any production package introduced during this work enters the same acceptance domain.
A firewall may contain backend conversion, but cannot hide category propagation, generic composition, refinement or a duplicate mathematical evaluator.
Mathematical directness is an ownership and semantic criterion, not a line-count target.

| ID | Work and complete consumer boundary | Needs |
| --- | --- | --- |
| `leaves-sets-order` | **Open.** **Reuse and extension boundary:** the existing `sets/` and `order/` modules and adapters, `cat/declarations.py`, `concrete.py`, `indexed.py` and `relations.py`; start from the retained set/order/cardinality consumers below. A usable generic construction is consumed directly; an unavailable one returns to its named A owner. Make all set/cardinal/natural and order leaves consume the accepted Cat interfaces: actual named-category implementation, set/map ownership, chosen enumerations, indexed constructions, relation/poset structure and finite algorithms through private engines. **Acceptance:** generic inherited improvements propagate through the named functors without coordinated leaf edits; finite-poset outputs retain their ambient carrier/inclusion; infinite and nonenumerable inputs preserve their contracts; and every leaf/helper has only its local mathematics or backend conversion. Preserve the existing cardinality scope, #9/#10/#30, `sets`/`orders`/`indexed`, and the complete finite-poset consumers. | `kernel-cat-complete` |
| `leaves-algebra` | **Open.** **Reuse and extension boundary:** the existing `algebra/` modules and adapters, `cat/monoidal.py`, `structured_objects.py`, `modules.py`, `bimodules.py`, `base_change.py` and universal presentations; retain the existing algebra consumers below. Extend the named domain functor or backend binding only after exercising its shared owner. Converge the algebra leaves for groups, additive objects, rings, modules/bimodules, tensors, actions, free objects and presentations, relative algebras and scalar change. Express their mathematics using supplied monoidal structures, named functors and retained universal presentations; move residual generic assembly to its A owner and keep Sage/GAP/CAP/OSCAR coordinate work behind private adapters. **Acceptance:** scalar-general and noncommutative examples, exact additive Homs, distinct outer-ring relative tensors, relation-respecting/non-respecting factors, free-algebra nonidentity substitutions and genuine forgetful maps retain their full behavior. Inspect every algebra module/helper in this family, including the additive coordinate boundary; a smaller façade over unchanged propagation machinery does not satisfy it. Preserve #21–#23/#40/#42–#47 and the retained module/algebra completion consumers. | `kernel-cat-complete`, `leaves-sets-order` |
| `leaves-geometry` | **Open.** **Reuse and extension boundary:** the existing `geometry/` modules, particularly `sheaves.py` and ringed-space composition, through `cat/constructions.py` products, equalizers, colimits and mediators. Start from retained scheme/gluing consumers below and transfer each generic responsibility to that existing owner. Converge spaces, topological rings, presheaves/sheaves, stalks, ringed/locally ringed spaces, affine schemes and general scheme gluing. Compatible-section rings and maps use the generic products/equalizers/limits; induced stalk maps use retained colimit mediation; locally ringed spaces inherit ringed-space composition; chart comparisons and scheme mediators are actual owned maps/cells. **Acceptance:** the three-chart, multi-piece-overlap consumer retains sheaf/stalk actions, chart comparisons, points/base slices and a nonidentity mediator, and rejects an incompatible chart family. Every geometry helper and adapter respects the mathematical owner. Preserve #48–#50, general gluing and global-section equivalences, without narrowing scheme or represented-open domains. | `kernel-cat-complete`, `leaves-algebra` |
| `leaves-infinitary-consumers` | **Open.** **Reuse and extension boundary:** the existing local-field/adele/CW and arbitrary-index consumers below, through the same `cat/indexed.py`, `constructions.py`, supplied monoidal structures and private engines. Extend the actual diagram/action path; retain each domain and existing representation. Complete the same leaf standard for local fields, adeles and CW constructions, and integrate them with the accepted arbitrary-index set/module and free-algebra paths. Each construction supplies its actual diagrams, structure maps, topology, actions and comparisons to the shared owners; private engines handle available computations. **Acceptance:** arbitrary-stage `CP^infty` maps and cocone mediation, the full adelic restricted product with diagonal `QQ` and topology, infinite/nonenumerable module sums, and unbounded free-algebra words retain the existing public obligations. No finite prefix, chosen enumeration or bounded-degree replacement is accepted. | `kernel-cat-complete`, `leaves-sets-order`, `leaves-algebra`, `leaves-geometry` |
| `completion-current-head-acceptance` | **Open — integrated behavioral and architectural acceptance.** **Reuse and extension boundary:** the family consumers and declaration-derived law obligations accepted by A and the four B nodes; compose that evidence at one revision and run the full public paths. This node adds integration coverage, not a second implementation. At one exact revision, execute all four leaf families through the public API, together with the foundational #24/#32 universal constructions, #30 enumeration, #21 group presentations, #47 rings, #45 complete static projection and #51 execution-state gate. Review every production leaf and helper for mathematical directness and correct reuse, and audit the truth of its supplied comparison/inverse/compatibility data separately from the compiler's acceptance of declarations. Verify isolatable imports and backend conversion boundaries, and that generic-owner improvements propagate to downstream consumers. **Acceptance:** real nonidentity public operations, complete universal data, exact runtime/static domains and production architecture checks all agree; a representative subset or a passing scanner alone is insufficient. A failure returns to its generic A owner or concrete B leaf owner. | `leaves-sets-order`, `leaves-algebra`, `leaves-geometry`, `leaves-infinitary-consumers`, `completion-plan-state-gate` |
| `leaves-complete` | **Open — milestone B.** **Reuse and extension boundary:** the complete production-module classification and integrated B consumers; review every classified leaf/helper, including newly added packages, against its domain and the same generic contracts. Every production leaf satisfies its complete topic contract through the accepted shared foundation and consists predominantly of direct mathematics plus private backend wiring. No residual generic implementation or lifecycle knowledge is hidden in a leaf helper/firewall, and all required per-leaf mathematical audits and integrated consumers are satisfied at the accepted revision. Earlier `leaf-blackbox-boundary` or feature closures do not establish this all-leaf criterion. | `kernel-cat-complete`, `completion-current-head-acceptance` |
| `framework-complete` | **Open — delivery of A and B.** **Reuse and extension boundary:** the two accepted milestones, retained public issue contracts and existing publication workflow. Both milestones hold at the delivered revision, their existing GitHub work units are reconciled with the full original contracts and evidence, and the accepted full SHA is on `main` and reachable from `origin/main` for external consumers under the owner's publication policy. Publication records delivery; it is not a substitute for either milestone and is not authorized merely by this DAG edit. | `kernel-cat-complete`, `leaves-complete` |

### Retained implementation evidence

The following rows preserve their original IDs, prerequisites, acceptance text and revision-scoped status.
They provide inputs and regression obligations to A and B. In particular, `leaf-blackbox-boundary` records its earlier representative scope; it does not certify every current leaf or the strengthened coherence contract.
Source/static closure that deferred runtime remains only source/static evidence until the corresponding current consumer executes.
Historical helper extraction or relocation is not an architectural standard for new work.

<details> <summary>Earlier framework implementation and acceptance records</summary>

| ID | Work and complete consumer boundary | Needs |
| --- | --- | --- |
| `leaf-blackbox-boundary` | **Closed 2026-09-17 at `c2e75eeb`.** `just architecture-boundary` is green; representative Sets/order/algebra/geometry theory is free of categorical-propagation plumbing outside `Cat`/kernel; backend/native dependencies are isolated under owning `_firewall/` boundaries. Make the repository's fundamental premise true before extending production mathematics: `Cat`, `cat_kernel`, and `kernel` own categorical propagation and runtime assembly; production leaf declarations contain mathematical data, immediate structure functors, genuinely local operations/predicates, and calls through a narrow leaf-local `_firewall/` conversion boundary when backend engineering is required. `just architecture-boundary` is the cheap mechanical guard: Import Linter enforces direct dependency directions and Sage isolation; production ast-grep rules reject reliable compiler/runtime leakage, backend/native access outside `_firewall/`, post-construction refinement, inherited identity/composition rewrites, leaf cache/retention state, generic leaf compiler parameters, and compiled-role casts. Repair existing order/algebra/geometry specimens until representative leaves can be authored without knowledge of `refine`, identity/cache machinery, dynamic role classes, placement, or native registries. A generic inherited improvement (especially a Sets behavior) must be owner-local and propagate through retained categorical structure without coordinated edits in downstream leaves. Backend-specific conversion complexity may remain, but only behind the leaf firewall and with owned mathematical inputs/outputs. **Acceptance:** `just architecture-boundary` is green on production source; representative Sets/order, algebra, and geometry leaves have no categorical-propagation plumbing outside `Cat`/kernel; and a backend change is isolated to the owning leaf firewall rather than leaking runtime concepts into neighboring leaves. | none |
| `finite-poset-owner` | **Closed 2026-09-17 at `112128fa`.** `just architecture-boundary` is green; `FinitePosets()` is the exact underlying-set inverse image, finite iteration returns elements of the original ambient poset, and public monotonicity admission consumes an owned set morphism through the finite-poset firewall. Implement `FinitePosets()` and `FiniteTotallyOrderedSets()` as the finite property/leaf layer over the existing owned relation and poset calculus. Public morphism admission consumes an owned set morphism and its monotonicity proposition; no raw callable is the mathematical map. The private Sage boundary lowers the exact owned finite carrier/order, reconstructs canonical owned elements and decisions, and never returns Sage elements, Python iterators, or built-in result containers. Finite iteration returns elements owned by the original ambient poset. Remove the remaining stale “theorem-backed” implementation wording while touching this owner; the current specifications already use the single semantic-constructor model. | `leaf-blackbox-boundary`, `orders`, `sets` |
| `finite-poset-subobjects` | **Closed 2026-09-17 at `66c52457`.** Source review confirms `Posets().Subobjects(P).from_predicate(...)` owns the induced subposet and exact inclusion; the finite cover/interval/ideal/filter/extrema operations return owned finite subobjects, and runtime plus feature-specific static consumers landed with the unit. Contract: use `Posets().Subobjects(P).from_predicate(...)` for induced subposets and for all finite collection-valued algorithms. Lower/upper covers, open/closed intervals, principal ideals/filters, generated ideals/filters, common covers, minimal elements, and maximal elements return owned finite subobjects with their inclusions into the exact original carrier. Inputs that are mathematical collections are owned subobjects rather than generic Python iterables. Chainhood and antichainhood are expressed through the induced order and existing property/equality predicates. | `finite-poset-owner` |
| `finite-poset-invariants` | **Closed 2026-09-17 at `4612679d`.** Source review confirms the complete specified finite-poset invariant surface, owned cardinal results including empty zero, exact-carrier linear extensions, ranked/graded and bottom/top property owners, owned level families, private-engine lowering/reconstruction, and runtime/static consumers. Contract: Implement `covers(lower, upper)`, `height()`, `width()`, and `linear_extension()` with the result domains in `specs/ordered-sets.md`; height/width are owned cardinals and the empty case is zero, while a linear extension is an owned finite total order on the same underlying set and extends the source order. Add ranked/graded finite-poset property subcategories, `rank_of_element(x)` as an owned natural cardinal, predicate-defined level sets, and the rank of a nonempty finite ranked poset. Bottom/top remain operations of their matching property subcategories rather than partial methods. The decisive private-Sage consumer must lower and reconstruct the exact semantic objects and the static consumer must retain their ambient domains. Completion of this node is completion of the substantive #9 finite-poset contract. | `finite-poset-subobjects` |
| `module-free-general` | **Closed 2026-09-17 at `4a835c5a`.** Source review confirms scalar-general regular and finite free left modules use the exact ordinary module owner and retained module product/coproduct data; the owned basis family and injections/projections are retained mathematical data; matrices only specialize basis-indexed free maps; and the `M_2(F_2)` consumer distinguishes left from right scalar action while the feature static consumer pins scalar/action ownership. Generalize the current integer-only free-module work to the ordinary module contract over an owned ring/scalar monoid. Construct the regular module and finite `R^n` through owned module products/direct sums, retaining scalar side and action; do not define a module by a matrix or by `ZZ`-specific coordinates. A basis family and its injections are owned data, and matrix evaluation is only the finite chosen-basis specialization. Preserve a noncommutative scalar example so left/right confusion is observable. | `leaf-blackbox-boundary`, `modules`, `module-sums`, `rings` |
| `module-presentations-general` | **Closed 2026-09-17 at `9cb60f2a`.** Source review confirms the supplied matrix builds the scalar-side-correct `R^m -> R^n` map through the generic finite-free owner; the parallel zero map and actual module-category coequalizer are retained; the presented object stays in the exact supplied `ModuleCategory`; the universal projection and factorization come from that retained coequalizer; and the noncommutative `M_2(F_2)` consumer distinguishes the left-module coefficient convention, verifies a respecting factor and its defining equation, and rejects a non-respecting map. Runtime execution remains owned by the independent `completion-runtime-beta10` node and does not serialize source completion. | `module-free-general`, `universal`, `additive` |
| `module-input-adapters` | **Closed 2026-09-17 at `887f127d`.** Source review confirms `from_endomorphism_action` uses the supplied monoid morphism by restricting the retained tautological `End_C(X)` evaluation module, preserving the exact carrier and action endpoints; `from_sage_module` reconstructs the exact native `ZZ`-module carrier, addition, zero, retained basis coordinates, and scalar action through the algebra leaf firewall before entering the supplied ordinary `ModuleCategory`. No placeholder identity action or parallel public module class remains. The feature static consumer passes; native Sage execution remains owned by the independent `completion-runtime-beta10` node and does not serialize source completion. | `leaf-blackbox-boundary`, `modules` |
| `modules-general-complete` | **Closed 2026-09-17 at `76162d0d`.** The unified `sage_categories.algebra.modules` façade now exposes the generic module owner together with scalar-general free modules, presentations, universal factorization, and native-ingestion adapters without introducing a second module representation. Source review confirms the noncommutative `M_2(F_2)` consumer retains the exact supplied scalar monoid, distinguishes the left-module coefficient side, and produces a nonidentity universal factor satisfying the defining equation; the façade and general presentation source contain no `ZZ`/integer-presentation fallback. The feature static consumer passes with the scalar input, module owner, free/presented objects, morphism endpoints, factor, and adapter result all retained in their exact public domains. Native Sage execution remains owned by the independent `completion-runtime-beta10` node and does not serialize source completion. | `module-presentations-general`, `module-input-adapters` |
| `algebras-relative-owner` | **Closed 2026-09-17 at `8a243f89`.** Source review confirms `Algebras(R,C)` is a distinct retained base-relative category whose sole immediate structure functor is an equivalence to `Monoids(V_R)` with a retained strict inverse; its selected-module route reads the exact monoidal structure selected on `Modules(R,C)`, while the noncommutative route consumes the supplied `(R,R)`-bimodule monoidal structure. Multiplication, unit, and algebra-map preservation stay owned by generic `Monoids`, and the retained composites reach the exact left-module owner, ambient category, and inherited set carrier without a parallel algebra representation or `EndofunctorAlgebras` alias. The feature static consumer passes; native Sage execution remains owned by the independent `completion-runtime-beta10` node and does not serialize source completion. | `leaf-blackbox-boundary`, `relative`, `modules`, `rings` |
| `algebras-relative-consumers` | **Closed 2026-09-17 at `60f0109c`.** Source review confirms the relative owner carries the dual-number algebra over `F_5`: `epsilon^2=0`, the algebra endomorphism `epsilon -> 2 epsilon` has the explicit inverse `epsilon -> 3 epsilon`, and the presentation equivalence round-trips both the algebra object and this nonidentity automorphism. Multiplication and unit are actual morphisms in the supplied relative bimodule category with the expected left-module endpoints. The `R=M_2(F_2)` consumer uses the diagonal `R -> R x R`, retains distinct left and right actions on the same bimodule, detects the noncentral `E12` action, and evaluates balanced componentwise multiplication without replacing the noncommutative base by a commutative convention. The feature static consumer retains the base, supplied monoidal structure, left-module owner, presentation functor, algebra object, and multiplication/unit morphism domains. Native Sage execution remains owned by the independent `completion-runtime-beta10` node and does not serialize source completion. | `algebras-relative-owner` |
| `algebra-free-presentations` | **Closed 2026-09-17 at `4988e63c`.** The free/presented layer now lives on the exact base-relative `AlgebraCategory`: the existing native `ZZ<...>` computation is reconstructed as an algebra object rather than serving as the definition, generator-image maps are algebra morphisms through the ordinary owner, and relation data are actual parallel algebra maps. The decisive presentation `ZZ<x,y>/(y-x^2) = ZZ[x]` is retained as an algebra-category coequalizer with its quotient map and universal factor; a nonidentity relation-respecting assignment factors through it while the identity assignment is rejected. The one-generator quotient supplies the commutative polynomial case, and a degree-101 monomial remains in its genuine infinite-rank underlying module. Feature syntax/Ruff, static projection, diff checks, and the 7/7 architecture boundary pass; native Sage execution remains owned by the independent `completion-runtime-beta10` node and does not serialize source completion. | `algebras-relative-consumers`, `modules-general-complete`, `universal`, `rings` |
| `algebras-general-complete` | **Closed 2026-09-17 at `ce089926`.** `sage_categories.algebra.algebra_objects` now exposes one base-relative public surface for `Algebras(R,C)`, free and presented algebra evaluators, quotient factors, forgetful composites, and scalar change. Restriction along an owned ring map `R -> S` acts functorially from `Alg_S` to `Alg_R`: both bimodule actions are restricted through the supplied scalar map, multiplication descends through the retained `tensor_R -> tensor_S` balancing map, and the unit is the original scalar structure map composed with `R -> S`. The terminal consumer restricts the regular `F_2`-algebra along `ZZ -> F_2`, checks its exact `ZZ` action and unit, and constructs a nonidentity generator permutation of `ZZ<x,y>` through the same owner. Together with the closed relative and presentation consumers, ordinary commutative-base, noncommutative-base, free, quotient, scalar-change, and nonidentity-map paths all use the one `AlgebraCategory`; the old `ZZ<...>` implementation is only a concrete evaluator. Feature syntax/Ruff, exact static consumers, diff checks, and architecture 7/7 pass; native Sage execution remains owned by the independent `completion-runtime-beta10` node. | `algebra-free-presentations` |
| `sheaf-stalks` | **Closed 2026-09-17 at `0491e784`.** Finite represented stalks are now the actual owned colimits of the section-ring diagram over the opposite of the induced neighborhood poset; the public stalk is the commutative-ring apex itself, not a wrapper. The finite topology's least neighborhood supplies an evaluator for that universal construction while every represented neighborhood retains its canonical germ as the corresponding colimit leg. `ringed_stalk_map` builds the induced map `O_{Y,f(x)} -> O_{X,x}` from the ringed-space sheaf natural transformation and source germs, then factors it through the target stalk presentation. The Sierpinski consumer distinguishes the `Z/2` stalk at the open point from global `Z/4` sections via a nonidentity germ, and a three-ring consumer verifies induced stalk maps compose. Feature syntax/Ruff, generic open-key static typing, diff checks, and architecture 7/7 pass; native Sage execution remains owned by the independent `completion-runtime-beta10` node. | `finite-poset-invariants`, `spaces-sheaves`, `rings`, `universal` |
| `locally-ringed-spaces` | **Closed 2026-09-17 at `ae5fc856`.** `LocallyRingedSpaces()` is now an owned layer over `RingedSpaces()` with one faithful forgetful functor carrying the exact ringed-space object and morphism. Objects retain a supplied local-ring proposition on each generic stalk; morphisms retain a supplied locality proposition on each exact induced stalk map from `ringed_stalk_map`, so no leaf-local local-ring decision algorithm or parallel stalk representation is introduced. Identity and composition are assembled from the retained continuous maps and sheaf natural transformations, with stalkwise locality inherited by the standard identity/composition theorem for local homomorphisms. The finite Sierpinski field-sheaf consumer uses the owned open-poset/stalk calculus, exercises a nonidentity continuous map, exact stalk maps, forgetful images, identity, and composition. Feature syntax/Ruff, exact static consumers, diff checks, and architecture 7/7 pass; native Sage execution remains owned by the independent `completion-runtime-beta10` node. | `sheaf-stalks` |
| `affine-global-sections` | **Closed 2026-09-17 at `b88eb8af`.** `GlobalSections : AffineSchemes().op() -> CRing` reads the root value of the retained structure sheaf, affine morphisms act by their exact contravariant pullback, and the ring/affine round trips carry executable natural-isomorphism components. The consumer composes two nonidentity `Spec` maps through prime preimages and global sheaf sections, represents supplied primes directly without enumerating the spectrum, and distinguishes `Spec(F_5[e]/(e^2))` from `Spec(F_5)` while retaining the nonzero nilpotent section. Static projection retains the functor variance and comparison transformation types. | `locally-ringed-spaces`, `affine`, `rings` |
| `scheme-locally-ringed-owner` | **Closed 2026-09-17 at `4b421aae`.** `Schemes()` is now the `Scheme` full property subcategory of `LocallyRingedSpaces()`: affine schemes enter as the exact locally ringed-space values and affine morphisms as the exact locally ringed-space morphisms. Each scheme retains an affine open cover carrying its local-affineness witnesses, while the forgetful functors to locally ringed, ringed, and topological spaces are retained composites of the existing owners. OSCAR covered schemes are now private native realizations attached to those public values rather than the public scheme identity. The full-subcategory owner gained the generic same-arrow restriction operation needed to place an ambient morphism without reconstructing leaf data. Ruff, Python compilation, diff checks, and architecture 7/7 pass; native Sage execution remains owned by the waiting `completion-runtime-beta10` node because `SAGE_BIN` is unset in the current shell. | `affine-global-sections` |
| `scheme-general-gluing` | **Closed 2026-09-17 at `0669c776`.** `Schemes().glue_affines(...)` now retains arbitrary finite affine chart families, complete pairwise overlaps whose represented opens may require several affine pieces, inverse overlap maps and the checked OSCAR cocycle. The resulting scheme is the owner-composed locally ringed space with quotient topology and compatible-section sheaf, retains its affine open immersions as the local-affineness witness, and retains an explicit natural-isomorphism comparison between the glued sheaf restricted to each chart and that chart's affine structure sheaf. `Schemes().gluing_mediator(source, target, chart_maps)` consumes one compatible scheme map per retained chart and returns the unique retained scheme morphism, with its underlying continuous, sheaf, stalk, and native covered-scheme actions reconstructed on the exact owned endpoints. The three-chart consumer exercises multi-piece overlaps, restrictions, chart comparisons, quotient points/stalks, and a nonidentity compatible-family mediator; the feature-only static projection passes without absorbing the concurrent generated-stub rewrite. Python 3.14 syntax, Ruff, Julia parse, diff checks, `just architecture-boundary`, `just plan-state`, and static mypy pass. OSCAR validation now has an opt-in per-request timeout so a worker/GAP startup failure is bounded; exact integrated runtime execution remains owned by `completion-runtime-beta10`. | `scheme-locally-ringed-owner` |
| `scheme-points-and-base` | **Closed 2026-09-17 at `6de3cbf9`.** Scheme point semantics now use three existing owners with no parallel point record: `X.underlying_points()` is the carrier of the underlying topological space; `X.valued_points(A)` is the exact fixed Hom category `Mor(Schemes())(Spec(A), X)`; and `X.categorical_points()` is `Mor(Schemes())(Spec(ZZ), X)`, supported by a single owned OSCAR-backed `integer_ring()` constructor. A supplied structure map `X -> S` enters `Schemes().SliceOver(S)` through `X.over(S, f)`, so the defining arrow and all morphisms over the base are the ordinary generic slice data. The consumer distinguishes a prime-ideal topological point from an `F_5`-valued scheme morphism, pins the categorical-point source to `Spec(ZZ)`, and verifies the structure map and identity over the base through the generic slice. Python 3.14 syntax, Ruff, Julia parse, diff checks, `just architecture-boundary`, `just plan-state`, and the feature-only static consumer pass; exact integrated runtime execution remains owned by `completion-runtime-beta10`. | `scheme-locally-ringed-owner` |
| `geometry-owner-reuse` | **Closed 2026-09-17 at `5a6bc0f8`.** The reopened scheme gluing now delegates instead of maintaining a parallel geometry stack. Compatible-family section rings, componentwise restrictions, chart projection/lift maps, chart sheaf comparisons, and mediator sheaf maps are owned by the generic descent operations in `geometry/sheaves.py`; the quotient carrier/topology, chart maps, and quotient mediator are owned by `Sets` and `TopologicalSpaces`. The scheme leaf supplies only its affine transition/open data and assembles the existing `RingedSpaces`/`LocallyRingedSpaces` values. The named local scheme helpers and the OSCAR `covered_open_family_compatible`/`covered_component_intersection`/`finite_gluing_mediator` layer are gone. The firewall now reconstructs pairwise overlap restrictions from OSCAR's `StructureSheafOfRings`/`restriction_map` over its native `Covering`/`Gluing` data, and packages compatible chart maps directly in OSCAR's `CoveringMorphism`/`CoveredSchemeMorphism`; source comments record why the small overlap adapter is needed. The general-gluing consumer now pins the actual `TopologicalSpaces`, `RingedSpaces`, and `RingSheaf` owners. Python 3.14 syntax, Julia parse, Ruff, diff checks, `just architecture-boundary`, `just plan-state`, and both gluing/point-base static consumers pass (the latter against a non-mutating feature shadow because the inherited `schemes.pyi` regeneration is still in flight). Live Sage execution remains an explicit obligation of `completion-runtime-beta10`; no ad-hoc OSCAR script is used as completion evidence. | `scheme-locally-ringed-owner` |
| `gluing-mediator-compatibility` | **Closed 2026-09-17 — source/static acceptance under `completion-runtime-beta10`.** `covered_scheme_morphism_from_chart_maps` now leaves OSCAR's native `CoveringMorphism` overlap checks and `CoveredSchemeMorphism` covering-admission checks enabled; no replacement overlap algorithm was added. The native boundary regression and the public three-chart, multi-piece-overlap consumer require immediate rejection when only the third chart sends `u` to `x+1` instead of `x`. The public consumer repeats rejection after constructing and retaining the compatible mediator, then preserves its existing inverse-image, sheaf, point, and stalk assertions. Python 3.14 syntax, Julia parse, feature Ruff, the exact general-gluing static consumer, diff checks, `just plan-state`, and `just architecture-boundary` (7/7 contracts) pass. Integrated execution of both regressions remains required by `completion-runtime-beta10`; native behavior was not executed in the source-only environment. | `geometry-owner-reuse` |
| `projective-line-general-consumer` | **Closed 2026-09-17 — source/static acceptance under `completion-runtime-beta10`.** `projective_line` now uses `Schemes().glue_affines`, the general chart sheaf comparisons, and the checked gluing mediator; the separate covered-scheme category, two-chart record, and private projective-line presheaf are removed. The public `F_5` consumer requires both chart restrictions, the actual overlap sheaf action, zero/infinity exchange, `2 <-> 3`, the inverse equations, and the point/sheaf round trips. The coefficient structure map lands in the exact `Spec(F_5)` base, acts on both chart section rings, and places the scheme and swap in the ordinary slice with both inverse equations. Chart inclusions and slice construction have their exact owner types. Python 3.14 syntax, Julia syntax, feature lint, and three source-projected boundary consumers (including `projective_line_general.py`, with `Any` rejected), diff checks, `just plan-state`, and `just architecture-boundary` pass. The inherited staged projection is untouched; full compiler projection and execution of the public consumer remain required by `completion-runtime-beta10`, not claimed by this source checkpoint. | `scheme-general-gluing`, `scheme-points-and-base`, `geometry-owner-reuse`, `gluing-mediator-compatibility` |
| `geometry-general-complete` | **Closed 2026-09-17 — source/static acceptance under `completion-runtime-beta10`.** Locally ringed spaces retain their open-key parameter through both constructors, their ringed/topological images, and their sheaves; affine and finite-glued schemes retain their distinct owned open-key types. Ringed and locally ringed morphisms expose the exact continuous-map and sheaf-map types. The existing projector now specializes a generic role's provider bases even when its category is nongeneric; its regression fails on the prior source and passes with the repair. The three-chart public consumer observes the same scheme and mediator through the full forgetful chain, with the existing sheaf, point, stalk, and incompatibility assertions retained. The displaced two-chart backend path is removed. Finite sheaf gluing accepts the empty cover exactly on the empty open and still verifies uniqueness; the public fixtures use the zero ring there and reject nonunique empty-open gluing and incompatible families. Seven feature-boundary static consumers, strict concrete P1/open-key probes, three projector regressions, Python 3.14 and Julia syntax, feature lint, diff checks, plan-state, and architecture boundaries pass. Full compiler projection and integrated public-consumer execution remain with `completion-runtime-beta10`; inherited projection changes are preserved rather than hand-edited. | `projective-line-general-consumer` |
| `completion-plan-state-gate` | **Closed 2026-09-17 — execution-gate acceptance; Sage runtime remains independent.** `scripts/plan_state.sh` defaults to the current issue-DAG owner, checks the governing plan and exact native repository/root, and validates the canonical TODO prerequisite graph with missing-link, duplicate, cycle, malformed-row, and premature-closure rejection. Explicit `phase-managed` mode reads the historical phase/core cards without mutating them, requires accepted revisions and satisfied prerequisites, rejects active archived phases, and preserves the single-active-phase and core-before-production-leaves contracts. The plan's completed remediation slice does not require closure of broader issue contracts. Twenty executable regressions exercise valid/invalid states in both models and preservation of archived records. Shell/Python syntax, feature lint, current 302-node TODO validation, native issue validation, and architecture checks pass. The real `just test-push` path proceeds through plan-state, rule coverage, and all seven architecture contracts before stopping solely at `_sage-preflight` because `SAGE_BIN` is unset; no actual push or mathematical/runtime acceptance is claimed. | none |
| `completion-runtime-beta10` | **Closed 2026-09-18 — declared Sage/Python runtime and Catlab bootstrap execute at the retained build.** The retained external `dzackgarza/sage@develop` Meson/Meson-Python installation is Sage 10.10.beta10 on Python 3.14.3 and imports `sage` through the declared `SAGE_BIN=$HOME/sage-mypy-plugin/sage-stubs/sage-src/sage`; no rebuild, reconfiguration, second Sage, or distribution `make` route was used. Fresh inspection found about 1.8 GiB free on `/` and identified the remaining root-backed JuliaPkg/cache paths before execution. The mutable Catlab project, Julia compiled depot, XDG cache, and package temporary files were kept under the existing `/dev/shm/sage-src-meson-20260917T1854Z` staging, with `JULIA_CONDAPKG_BACKEND=Null` and `JULIA_PYTHONCALL_EXE` bound to the declared Sage Python; the pre-existing root Catlab manifest and Julia usage log did not change. Serial Julia precompile completed 17 dependencies in 579 seconds, including `PythonCall 0.9.35`, `GATlab 0.2.4`, and `Catlab 0.17.6`, then a fresh process loaded all three and reported `CATLAB_BOOTSTRAP_OK`. Repository import/bootstrap through the declared Sage launcher reused that staged project without changing its Project/Manifest and reported `REPOSITORY_BOOTSTRAP_OK 3.14.3 0.1.0`. The unchanged targeted runner `bash scripts/run_sage_test_case.sh tests/kernel/test_declared_python_runtime.sage test_declared_sage_python_runtime` then passed its selected test under Python 3.14.3 and exited 0; its small `mktemp` tree used a dedicated root-backed `/tmp/sage-categories-beta10-runtime` because `gio trash` cannot clean a worktree on `/dev/shm`. Root headroom remained about 1.7 GiB after acceptance, and no Julia/Sage runtime process remained. [Current and superseded runtime evidence](COMPLAINTS.md#commit-qc-loses-repository-runtime-and-workspace-dependency-bindings). | none |
| `completion-static-integration` | **Closed 2026-09-18 — integrated semantic projection accepted.** The repository projector now runs parse-only stubgen outside the Sage import process, retains only category-graph modules, threads inherited category roles through intermediate owners, projects exact morphism endpoints and functor action results, and completes source-declared property-containment edges even before a concrete property subcategory has been instantiated. The complete projection regenerates through `just generate-stubs` under the declared beta10 runtime and passes both Ruff contracts with 40 retained stubs. The integrated static consumers preserve exact functor endpoints, fixed Hom source/target, refinement identity, module/algebra scalar parameters, finite-poset result domains (including graded→ranked operations), open-key parameters, stalk maps, affine variance, scheme base/cover parameters, and morphism result categories; the 14-file mypy batch is green. The named sequential-category compiler regression also passes under the declared Sage runtime. | `finite-poset-invariants`, `modules-general-complete`, `algebras-general-complete`, `geometry-general-complete`, `completion-runtime-beta10` |

</details>

## Native-engine remediation and consolidation DAG

These historical records retain their IDs and obligations for reuse and regression analysis.
They do not supply an alternative active work order.

<details> <summary>Accepted native-engine remediation and consolidation records</summary>

`A` in `B`'s Needs column means `A -> B`: B consumes the stated output of A. `none` means no prerequisite node in this table; existing mathematical inputs and the plan's retained issue contracts still apply.
Acceptance belongs to the linked contract, not the short node name.
Multiple prerequisites are conjunctive.

| ID | Work and full obligation in the governing plan | Needs |
| --- | --- | --- |
| `runtime` | **Closed.** Section 19.1: runtime bindings, owned reconstruction, exact installation, earlier objects and selected maps. **Acceptance:** `tests/engines/test_runtime_identity_retention.sage::test_runtime_identity_retention` preserves distinct owned identities, exact native-map endpoints and selected presentations sharing an apex; `tests/kernel/test_runtime_selected_actions.sage::test_runtime_selected_actions` preserves selected-action order, earlier objects/arrows and cached selected-functor images; the exact-category implementation and role-surface consumers pass after augmentation | none |
| `sets` | **Closed.** Section 19.2: restore general Sets products, including rule-defined integers, represented reals and mixed inputs, while retaining finite native operations | `sets-mixed`, `sets-finite-native` |
| `functors` | **Closed.** Section 19.2: native categories, both functor actions, transformations, compositions, whiskerings and nonenumerated sources. **Acceptance:** `tests/engines/test_catlab_functor_calculus.sage::test_catlab_functor_calculus` passes with native category/functor/transformation retention over an unenumerated source and exercises object/morphism actions, vertical and horizontal composition, and both whiskerings; `tests/engines/test_catlab_functor_calculus.sage::test_horizontal_interchange_uses_nonidentity_components` proves horizontal interchange on nonidentity commuting components; `tests/kernel/test_composite_inheritance.sage::test_composite_inheritance_uses_the_composite_image` passes for the selected-composite inheritance consumer | none |
| `paths` | **Closed.** Section 19.2: complete typed presented paths and native finite universal-construction operation families. **Acceptance:** `tests/engines/test_fp_categories_execution.sage` passes for typed endpoints, identities, composition, native normalization, justified inverses, finite/nonfinite path families, and parallel relations (including a length-two commuting square); `tests/engines/test_category_products_execution.sage::test_native_category_product_structural_data` and `tests/engines/test_category_limits_execution.sage::test_native_structural_limit_of_constant_arrow_diagram` pass for native finite structural products/limits; `tests/kernel/test_universal_constructions.sage::test_pushout_composes_arrows_from_both_factors` and `::test_coequalizer_identifies_functor_images_of_a_generator` pass for presented colimits and quotient manipulation; `tests/engines/test_presented_terminal_selection.sage::test_two_isomorphic_vertices_are_both_terminal_but_one_is_selected` preserves multiple terminal choices with one retained selection | none |
| `universal` | **Closed.** Section 19.2: retained diagrams, chosen apex/legs/mediators, nonstandard indexing, opposites and predicate subobjects (#30/#24/#41; intrinsic #32). **Acceptance:** `tests/kernel/test_intrinsic_universal_presentations.sage` retains distinct presentations sharing an apex, dual/opposite universal data, nonstandard discrete indices, predicate-subcategory ownership, legs and executable mediators without reconstructing a second object system | `sets`, `functors`, `paths` |
| `indexed` | **Closed.** Section 19.2: full infinite/nonenumerable indexed constructions and sequential colimits with maps and their specified equality. **Acceptance:** `tests/sets/test_indexed_products.sage` exercises products and coproducts over `NN` and a nonenumerable real index with supplied projections/injections and independent mediators; `tests/sets/test_sequential_colimits.sage` retains arbitrary-stage representatives, common-later-stage equality/unknown semantics, an independent cocone descent, and the nonidentity colimit map induced by a compatible stagewise shift, including its universal stage-leg equations | `sets`, `functors` |
| `diagrams` | **Closed.** Section 19.3: symbolic, supplied monoidal and cell interpretation, including nonstrict comparisons and noninvertible ordinary arrows. **Acceptance:** `tests/engines/test_discopy_nonstrict_model.sage` exercises retained nonstrict comparison maps through DisCoPy; `tests/kernel/test_scaled_associator_pentagon.sage` routes the public supplied monoidal structure through that interpretation, preserves the scaled associator pentagon distinction `8 != 4`, and connects interpreted semantic arrows to homotopy-core cells so a noninvertible ordinary arrow remains directed while the retained associator component is invertible; `tests/engines/test_catlab_functor_calculus.sage::test_horizontal_interchange_uses_nonidentity_components` keeps a primitive noninvertible transformation directed; `tests/engines/test_homotopy_cells.sage` retains native boundaries, identities, composites and typechecking | `functors` |
| `refinement` | **Closed.** Section 19.4: same-object refinement (#31), with existing inherited operations. **Acceptance:** `tests/kernel/test_refinement_descendant.sage::test_refining_intermediate_category_preserves_existing_placed_descendant` passes while preserving the exact already-placed object and identity morphism across an intermediate-category refinement, including inherited object and morphism operations from the middle, upper and property declarations | none |
| `inverses` | **Closed.** Section 19.4: retained inverses (#33). **Acceptance:** `tests/kernel/test_retained_inverses.sage::test_retained_inverses_compose_in_reverse_order` passes with explicitly retained inverse pairs, retained inverses of composites, involutive `inverse()`, and pointwise two-sided round trips; `tests/engines/test_fp_categories_execution.sage::test_native_finite_presented_category_paths` also preserves the native presented inverse in the walking isomorphism | none |
| `isofibrations` | **Closed.** Section 19.4: composite isofibrations and generic state transport (#35). **Acceptance:** `tests/kernel/test_composite_isofibration_transport.sage::test_composite_isofibration_transports_generic_state` passes: a composite of two declared isofibrations retains the `Isofibrations()` property, source objects inherit state from both selected targets in the declared chain, and public application of the retained composite returns the same cached target image | none |
| `named` | **Closed.** Section 19.4: named projections and composites (#39), consuming the required structure and transport. **Acceptance:** `tests/algebra/test_named_functor_calculus.sage::test_named_restriction_retains_renaming_and_both_actions` passes for retained projection inverses, composite named restrictions, both object/morphism actions, isofibration inheritance and nonidentity maps; `::test_named_operations_keep_their_images_after_refinement` passes with additive and multiplicative selected targets, retained images and operations after refinement | `refinement`, `inverses`, `isofibrations` |
| `additive` | **Closed.** Section 19.4: native additive owner (#40 and additive #32), exact Ab Homs, both selected presentations and forgetful maps. **Acceptance:** `tests/algebra/test_additive_biproduct_owner.sage` constructs the required `ZZ + ZZ/4` biproduct with distinct retained product/coproduct presentations, CAP-retained projections/injections and nonidentity universal maps, the composite `n -> 3n mod 4`, and its actual forgetful images; `tests/algebra/test_additive_hom_owner.sage` pins exact `Ab` Hom ownership and a nonidentity carrier image; `tests/algebra/test_abelian_coequalizer_cap.sage` retains the native selected quotient, projection and mediator | `named`, `paths` |
| `tensor` | **Closed.** Section 19.4: ordinary tensor (#42), induced maps, universal factors and comparisons. **Acceptance:** `tests/algebra/test_abelian_tensor_cap.sage` retains CAP tensor objects, a nonidentity induced tensor map, a quotient-crossing universal mediator, and CAP-owned associator/unitors with retained inverses; `tests/algebra/test_abelian_tensor_scaffold.sage` exercises free/torsion tensors, the universal biadditive factorization, unit comparisons, associativity and a rejecting counterexample for a rule that does not descend. Presented comparison maps use MonoidalCategories WithGiven operations rather than the former coordinate rebracketing path | `additive` |
| `relative` | **Closed.** Section 19.4: relative tensor (#43), distinct outer rings, noncommutative middle ring, both actions and balanced factors. **Acceptance:** `tests/algebra/test_relative_tensor_scaffold.sage` computes rows tensor columns over the noncommutative middle ring `M_2(F_2)`, factors a nonidentity balanced dot product, descends both outer actions, and instantiates the full `(R,S) x (S,T) -> (R,T)` contract with distinct outer monoids `R=ZZ` and `T=F_2`; `tests/algebra/test_relative_tensor_maps_scaffold.sage` exercises a nonidentity induced map and executable unit isomorphisms; `tests/algebra/test_bimodule_tensor_monoidal_scaffold.sage` exercises the descended associator and unit coherence in the endomorphism case | `tensor`, `inverses` |
| `actions` | **Closed.** Section 19.4: supplied actions and module transport (#44), retaining distinct acting and acted-on categories and generic structured objects. **Acceptance:** `tests/algebra/test_module_transport_owners.sage` constructs a nonconstant supplied action `M x C -> C` with `M != C`, a generic monoid/module object, transports its action along a nonidentity retained isomorphism, checks the transported action square and observed values, retains the exact inverse in both directions, and transports back to the original module; the self-action case uses the same owner rather than a parallel implementation | `relative`, `named`, `diagrams` |
| `kan` | **Closed.** Sections 14 and 19.4: Kan, weighted, profunctor and relation consumers with actual diagrams and universal maps. **Acceptance:** `tests/kernel/test_universal_constructions.sage::test_kan_maps_factor_nonidentity_transformations` exercises pointwise left/right Kan values, arrow actions, units/counits and nonidentity universal factors; `tests/kernel/test_weighted_universal_maps.sage` exercises changing weights, categories of elements, weighted projections/injections and independent mediators, ends/coends and Yoneda; `tests/kernel/test_profunctor_composition.sage` exercises coend composition, nonidentity horizontal 2-cells and retained co-Yoneda unitors; `tests/kernel/test_regular_relations.sage` exercises relation images, horizontal composition, associativity and graph functor maps through the shared calculus | `universal`, `functors` |
| `orders` | **Closed.** Section 19.4 remediation slice of #41 and the #9/#10 order branch: selected squares, supplied inverse transport, indexed comparisons and undecided predicates. The broader finite-poset API in `specs/ordered-sets.md` remains owned by #9. **Acceptance:** `tests/order/test_poset_scaffold.sage` retains the selected product square and monotone mediator, rejects invalid/reversing relations, transports finite and predicate-defined infinite relations through supplied retained inverses without enumeration, and preserves indexed comparisons; `tests/kernel/test_predicate_set_subobjects.sage` retains true, false and genuinely undecided membership on predicate subobjects of an infinite ambient set | `universal`, `inverses` |
| `groups` | **Closed.** Section 19.5: group presentations (#21), relation arrows and universal factors. **Acceptance:** `tests/algebra/test_presented_groups.sage` retains a native free group, the relation/trivial parallel arrows, the selected quotient map, evaluation of presented words, a nonidentity universal factor through a relation-respecting assignment, rejection of a violated relation by Sage/GAP, and a nonenumerated free-group carrier as an ordinary object of `Groups(Sets)` | `named`, `paths` |
| `modules` | **Closed.** Section 19.5: module presentations and constructors (#22), scalar conventions, cokernel and supplied actions. **Acceptance:** `tests/algebra/test_integer_module_presentations.sage` constructs finite free left `ZZ`-modules, retains the actual relation-matrix module morphism and CAP cokernel projection, and factors a nonidentity relation-killing module map through that selected quotient; `tests/algebra/test_ring_module_scaffold.sage` retains two distinct supplied scalar actions; `tests/algebra/test_matrix_ring_scaffold.sage` preserves the standard left module for the noncommutative scalar ring `M_2(F_2)` rather than importing a commutative/right-module convention | `tensor` |
| `module-sums` | **Closed.** Section 19.5: arbitrary indexed module sums, finite support per element, injections and universal maps. **Acceptance:** `tests/algebra/test_indexed_free_modules.sage` constructs `ZZ^(S)` for both an infinite integer index and a nonenumerable predicate-defined real index without choosing an enumeration, retains finite-support elements at arbitrary supplied labels, and exercises the canonical injections plus an independent family of module maps whose unique mediator satisfies the coproduct equation | `indexed`, `actions` |
| `algebras` | **Closed.** Section 19.5: #46 then #23; full algebra presentations and infinite underlying-module consumer `R<x,y>`, retaining genuine forgetful maps. **Acceptance:** `tests/algebra/test_free_associative_algebra.sage` constructs native `ZZ<x,y>` as a monoid object in the selected bimodule tensor category, retains the full free `ZZ`-module on all finite words with no degree cutoff, distinguishes `xy` from `yx`, retains a 101-letter word, and sends `x -> x+y`, `y -> y` to an owned algebra endomorphism whose image through the genuine module forgetful functor satisfies `xy -> xy+yy` on the same underlying module | `relative`, `module-sums` |
| `rings` | **Closed.** Section 19.5: rings/localization (#47), owned maps and native algebra integration. **Acceptance:** `tests/algebra/test_commutative_ring_presentations.sage` constructs OSCAR-backed prime fields, polynomial and quotient rings, preserves a nonzero nilpotent in the dual numbers, retains quotient/polynomial maps, principal localization and its universal extension, prime localizations and the induced owned stalk map with exact endpoints; `tests/algebra/test_ring_scaffold.sage` preserves the separate noncommutative ring and rejects nonunital maps rather than narrowing the ring contract | `named`, `additive` |
| `spaces-sheaves` | **Closed.** Section 19.5 and #48: spaces, topology/maps, sheaf values and restrictions on their proper domains. **Acceptance:** `tests/geometry/test_topological_spaces.sage` retains continuous maps and contravariant inverse-image functors on exact open categories; `tests/geometry/test_ring_presheaf.sage` retains section rings and owned restriction maps only on inclusions of opens and checks contravariant composition; `tests/geometry/test_ring_sheaf_gluing.sage` glues a compatible finite cover to its unique global section; `tests/geometry/test_ringed_spaces.sage` retains the continuous map and natural sheaf action with exact section-ring endpoints | `rings` |
| `affine` | **Closed.** Sections 17 and 19.5: affine branch (#49), contravariant Spec, native pullbacks and induced maps. **Acceptance:** `tests/geometry/test_affine_stalk_maps.sage::test_affine_map_retains_prime_image_and_stalk_map` constructs a nonidentity ring pullback, checks `Spec` reverses its exact endpoints, retains the native OSCAR affine morphism and coordinate-ring identities, maps a prime-spectrum point, and retains the induced local-ring map on exact stalk endpoints; `tests/geometry/test_affine_structure_sheaf.sage` retains native principal-open restrictions and their composition | `spaces-sheaves` |
| `gluing` | **Closed.** Sections 17 and 19.5: gluing (#50), projective-line charts, structure sheaf and chart-swap action. **Acceptance:** `tests/geometry/test_projective_line_gluing.sage::test_projective_line_two_chart_gluing_and_swap` constructs the OSCAR-backed two-chart projective line, retains native chart inclusions and the native chart-swap endomorphism on exact owned endpoints, checks the structure-sheaf section rings and overlap restrictions, and verifies the swap acts by inversion on the overlap coordinate | `affine` |
| `topological-colimit` | **Closed.** Section 19.5: full CP^infty diagram, CW topology, inclusions and induced continuous maps. **Acceptance:** `tests/geometry/test_projective_infinity_colimit.sage::test_projective_infinity_retains_arbitrary_stages_and_mediator` retains finite skeleta and standard structure maps at stages 2, 37 and 137 with no prefix cutoff, distinguishes each finite skeleton from the colimit, retains the weak CW topology stagewise, and checks the nonidentity continuous map induced by compatible complex conjugations commutes with the colimit structure maps | `indexed`, `spaces-sheaves` |
| `adeles` | **Closed.** Section 19.5: full restricted product, topology, ring operations, component maps and diagonal QQ map. **Acceptance:** `tests/algebra/test_adeles_restricted_product.sage` retains the real factor and callable finite-place components over the full prime index, verifies the unit has nonzero integral components at arbitrarily supplied primes, checks the diagonal `QQ` map and exact component-map endpoints, verifies `1/5` is exceptional and nonintegral only at place 5 among the tested places, exercises ring addition/multiplication, and retains basic opens with finite exceptional data and continuous-operation preimages | `indexed`, `rings`, `spaces-sheaves` |
| `mypy-runtime` | **Closed.** Type checking now executes under the Sage QC profile: `ai-review-ci@33bb0a1` resolves workspace dependencies and selects `mypy-sage.ini`, `ab730f8` preserves the project stub path, and `tests/static/mypy_runtime_probe.py` pins exact Hom/refinement projection. With the repaired local `sage-stubs` source substituted at its declared dependency boundary, the unchanged parent gate reaches project mypy and reports project diagnostics, establishing that mypy starts and the configured category plugin loads. Published dependency and gate-tier blockers are tracked separately below | none |
| `sage-stubs-publication` | **Closed.** The validated `sage-stubs` repair through `f19dc7973149adf54b2754fae3e1364283f1c351` is published on `dzackgarza/sage-stubs@main`, and this repository's `uv.lock` now resolves the declared `sage-stubs@main` dependency to that exact public revision. **Acceptance:** the public dependency contains the validated parser/owner repairs and ordinary QC no longer needs the local dependency override to obtain them | none |
| `sage-gate-tier` | **Closed.** Central QC commit `ai-review-ci@d8babfc` moves Sage `_mypy` from `test-commit` to `test-push` while retaining the Sage mypy profile and `_sage-pytest`; the targeted central policy test passes and downstream repositories still delegate to the public central recipes | none |
| `stub-conformance` | **Closed.** `kernel/stub_generator.py` is reachable as a recipe, and the `.pyi` it emits satisfies this repository's own formatter and linter by construction. The projector owns stub lint conformance; current tracked stubs pass both default Ruff and the repository global Ruff/format contract (`e01593e`). | none |
| `lint-debt` | **Closed.** The hand-written `.py` Ruff debt is cleared at its owners, including retained-reference defects rather than suppressions or scope reduction. **Acceptance:** `uvx --from 'ruff>=0.16.5,<1' ruff check src tests` reports `All checks passed!` on the committed source and tests | none |
| `gate` | **Closed.** Section 19.6: the ordinary commit execution gate now passes from a clean checkout with the published `sage-stubs` repair, the Sage mypy tier moved to push, generated-stub conformance and hand-written Ruff debt closed, and no inherited `[known red: ...]` allowance. **Acceptance:** `just test-commit` under the tracked development `SAGE_BIN` completes the doctor/preflight, normalization, Sage syntax, Python syntax/Ruff and bypass checks successfully on the committed dependency boundary | `mypy-runtime`, `stub-conformance`, `lint-debt`, `sage-stubs-publication`, `sage-gate-tier` |
| `python-runtime` | **Closed.** Section 19.6: the repository targets the source-built `dzackgarza/sage@develop` runtime on Python 3.14, kept aligned with upstream Sage `develop` rather than a second stable/host Sage installation. The current baseline is Sage 10.10.beta10. Source syntax, static projection, local `SAGE_BIN`, and runtime acceptance all use that development runtime rather than carrying an older-Python source variant. OSCAR remains isolated from the package-global Catlab/GATlab Julia process. **Acceptance:** `tests/kernel/test_declared_python_runtime.sage::test_declared_sage_python_runtime` requires Python 3.14 and the Sage 10.10-or-newer development line; `tests/engines/test_oscar_process_boundary.py` checks the OSCAR process boundary | none |
| `static` | **Closed.** Sections 18 and 19.6: exact static projection (#45), developed alongside the same runtime consumers. **Acceptance:** `just generate-stubs` completes from the public recipe and the generated projection passes both repository Ruff contracts; `tests/static/test_stub_generator_ast.py` passes 42/42 including pruning stale imports and exact generated affine/gluing owner signatures; the tracked `assert_type` consumers retain constructor parameters, role/Hom ownership, refinement, action-owner distinctions, infinitary consumers and geometry endpoints on the native-backed public interface | `stub-conformance` |
| `host-headroom` | **Closed.** Long Sage/Julia consumer runs on this host are staged one named `.sage` test at a time through `scripts/run_sage_test_case.sh`: the runner preparses a temporary copy, suppresses only its top-level direct test invocations, and executes the selected function with verbose pytest progress under the then-declared Sage 10.9 / Python 3.14 runtime. **Historical acceptance evidence:** `test_set_scaffold.sage::test_rule_defined_infinite_set` completed in this shape under Sage 10.9 / Python 3.14.7. See COMPLAINTS.md, "Recovered Python-3.14 Sage runtime exhausts host headroom under public consumers" | none |
| `monodict-autofix` | **Closed.** The central Ruff owner now marks `SIM401` and `PERF102` unfixable at `ai-review-ci@8bfde79`, preserving Sage `MonoDict` membership/indexing and `.items()` forms even under `--unsafe-fixes`. **Acceptance:** the central policy test passes and an unsafe-fix reproducer leaves the `MonoDict`-compatible source unchanged; `tests/kernel/test_monodict_runtime.sage` pins the runtime API | none |
| `sets-integers` | **Closed.** Section 19.2 decomposition: rule-defined integer objects inside general Sets products. **Acceptance:** `tests/sets/test_set_scaffold.sage::test_rule_defined_infinite_set` passes under the declared runtime | `sets-core` |
| `sets-reals` | **Closed.** Section 19.2 decomposition: represented reals inside general Sets products. **Acceptance:** `tests/sets/test_represented_real_products.sage::test_represented_real_product` passes under the declared runtime | `sets-core` |
| `sets-mixed` | **Closed.** Section 19.2 decomposition: mixed-input products combining enumerated, rule-defined and represented factors. **Acceptance:** `tests/sets/test_mixed_input_products.sage::test_mixed_input_product` passes under the declared runtime | `sets-integers`, `sets-reals` |
| `sets-finite-native` | **Closed.** Section 19.2 decomposition: finite native operations retained unchanged across the restored general product. **Acceptance:** `tests/sets/test_set_scaffold.sage::test_finite_set_universal_maps` passes under the declared runtime and asserts retained native product, mediator and projection realizations | `sets-core` |
| `sets-core` | **Closed.** Section 19.2 decomposition: the general Sets product construction itself, before its factor kinds. **Acceptance:** `tests/sets/test_general_set_product.sage::test_general_set_product` passes under the declared runtime | none |
| `refactor-native-parent-membership` | **Closed.** Consolidate repeated Sage-element/native-parent identity checks in the indexed additive owner so membership and mediator/reindex assertions use one private boundary predicate. **Acceptance:** `_has_native_parent` owns the exact `SageElement`/parent identity test and all six indexed-additive call sites use it | `acceptance` |
| `refactor-axiom-declaration-lookup` | **Closed.** Consolidate runtime-class axiom lookup at the declaration owner rather than repeating `getattr`/dictionary-default probes. **Acceptance:** `_class_declaration` walks the exact runtime MRO once, and both axiom construction and inherited-axiom discovery use it; selected-functor state is read explicitly from the retained instance namespace | `acceptance` |
| `refactor-presented-native-presence` | **Closed.** Stop detecting retained presented-module realizations by deliberately triggering assertion failures. **Acceptance:** the owner-side native registries expose exact identity-based presence queries, and the CAP adapter uses those queries before reconstruction | `acceptance` |
| `refactor-presented-coordinate-access` | **Closed.** Collapse seven repeated lazy imports of the additive coordinate bridge in the CAP adapter to one private boundary accessor. **Acceptance:** `_coordinates` is the sole lazy owner import in `engines/presented_modules.py`, and all object/map/tensor conversion paths use it | `acceptance` |
| `refactor-cardinality-declaration-probe` | **Closed.** Remove exception-driven probing for the inherited `cardinality()` query when classifying discrete product shapes. **Acceptance:** the existing exact-MRO declaration reader handles arbitrary owned values, and `_nontrivial_discrete` binds the retained declaration explicitly or leaves the query undecided | `acceptance` |
| `refactor-compiler-state-mutation` | **Closed.** Make compiler projection/state mutation explicit instead of coupling lookup and insertion through consumed `setdefault`/default reads. **Acceptance:** inheritance/subtyping projection maps and `_own_classes` initialize retained state in a separate branch and then read the exact stored value | `acceptance` |
| `refactor-kernel-initializer-state` | **Closed.** Read declaration-local initializer state explicitly rather than probing function attributes with defaults. **Acceptance:** initializer installation distinguishes absent local `__init__` from an already wrapped initializer through exact class/function namespaces | `acceptance` |
| `refactor-julia-cache-root` | **Closed.** Make the OSCAR cache-root choice explicit rather than combining environment lookup and fallback through a default-value probe. **Acceptance:** `_oscar_cache_directory` reads `XDG_CACHE_HOME` only when present and otherwise selects the documented user cache root | `acceptance` |
| `refactor-stub-generator-state` | **Closed.** Consolidate accumulator state in the static projector and make fallback lookups explicit instead of mixing insertion with `setdefault`/default-bearing `dict.get`. **Acceptance:** set/list accumulators use the shared explicit `_set_bucket`/`_list_bucket` insertion helpers, declaration closure reads only existing keys, canonical-import and runtime-alias fallbacks are explicit, and provider ancestry pruning reads only retained relations | `acceptance` |
| `refactor-adele-open-state` | **Closed.** Make every adelic open carry a total real-place condition rather than using `None` as hidden state for whole/empty opens. **Acceptance:** `AdeleOpen.real_condition` is always callable; the whole and empty opens retain constant true/false conditions while their existing kind semantics stay authoritative | `acceptance` |
| `refactor-set-symbolic-state` | **Closed.** Replace nullable `_SetMap.rule` state by an explicit symbolic-rule sum, keeping raw callable evaluation independent of whether an exact SymPy representation exists. **Acceptance:** source and generated static projection contain no nullable symbolic-rule state, and existing symbolic composition/equality consumers retain their behavior | `acceptance` |
| `refactor-local-field-integral-state` | **Closed.** Replace nullable local-field integral-subring state by an explicit public sum distinguishing a selected integral subring from places where none is selected. **Acceptance:** `ExactLocalFieldPresentation.integers` is total, the static consumer sees the explicit sum, and `tests/algebra/test_adeles_restricted_product.sage::test_adeles_retain_components_ring_operations_and_diagonal` distinguishes the rational/real no-subring state from the retained p-adic valuation subring | `acceptance` |
| `refactor-construction-context-bookkeeping` | **Closed.** Consolidate the identical constructor-chain node bookkeeping shared by object, element, and morphism construction contexts. **Acceptance:** `_ConstructionContext` owns `run`/`assert_complete`; the three typed dataclass contexts retain only their distinct identities and state | `acceptance` |
| `refactor-stub-expression-names` | **Closed.** Consolidate duplicated AST name extraction in the static projector. **Acceptance:** `_dotted_name` owns recursive dotted spellings and `_base_name` owns parameterized-base names; annotation localization and category-role projection reuse those helpers | `acceptance` |
| `refactor-stub-function-annotations` | **Closed.** Remove duplicated annotation rewriting for synchronous and asynchronous function definitions in the static projector. **Acceptance:** `Annotations._visit_function` owns type-parameter/return rewriting and both AST visitor entry points delegate to it | `acceptance` |
| `refactor-discrete-product-coproduct-scaffold` | **Closed.** Consolidate the duplicated nontrivial-discrete family predicate and diagram/sequence scaffolding shared by products and coproducts without changing either category's public class graph. **Acceptance:** shared helpers own those three operations and the product/coproduct categories retain only their directional universal-construction logic | `acceptance` |
| `refactor-discrete-sequence-wrappers` | **Closed.** Remove the now-redundant product/coproduct `_sequence_diagram` wrappers after their common implementation moved to `_discrete_sequence_diagram`. **Acceptance:** both public constructors call the shared helper directly and no duplicate wrapper remains | `refactor-discrete-product-coproduct-scaffold` |
| `refactor-stub-category-role-projection` | **Closed.** Decompose the monolithic static category-role projection into named AST transforms with one owner each. Source-owner discovery, `CategoryDeclaration` defaults, static-role-helper creation, per-role defaults, generic role arguments, projected category bases, and local annotation rewriting now have separate owners. **Acceptance:** `_project_category_role_parameters` is orchestration rather than a 50+-branch implementation; `just generate-stubs` under the tracked stable `SAGE_BIN` is green with no tracked projection drift, and `tests/static/test_stub_generator_ast.py` passes 42/42 | `acceptance` |
| `refactor-cw-open-inclusion` | **Closed.** Consolidate the duplicated inclusion calculus for finite-stage and weak-colimit CW opens. **Acceptance:** `_named_open_inclusion` owns the shared identity/empty/whole/custom-rule cases and both open representations delegate to it without changing their retained stage/topology data | `acceptance` |
| `refactor-thin-composition` | **Closed.** Consolidate the identical endpoint-compatibility composition rule used by discrete and general thin shapes. **Acceptance:** `_thin_composite` owns the unique-arrow composition and both shape categories delegate to it while retaining their distinct admission/equality rules | `acceptance` |
| `refactor-gap-package-loading` | **Closed.** Centralize exact GAP-package load idempotence at the GAP runtime boundary instead of keeping per-adapter `_loaded` flags and `_load` wrappers. **Acceptance:** `load_packages` and `load_repository_package` cache their exact immutable package allocations, the finite category/product/limit, slice, and functor-category adapters call those shared owners directly, and `tests/engines/test_gap_runtime.sage` reads the returned GAP package records through their record components | `acceptance` |
| `refactor-construction-membership` | **Closed.** Consolidate the identical placement-based membership rule for chosen limit and colimit families. **Acceptance:** `_construction_membership_proposition` owns the POL-CAT-068 rule and both family classes delegate to it without changing their distinct universal-data ownership | `acceptance` |
| `refactor-adele-open-membership` | **Closed.** Split basic-open condition aggregation out of `AdeleOpen.contains` so the public method only owns kind/admission routing. **Acceptance:** `_adelic_open_conditions` owns the three-valued real/finite condition fold and `AdeleOpen.contains` is below the repository's complexity threshold without changing restricted-product semantics | `acceptance` |
| `refactor-local-field-valuation` | **Closed.** Split rational valuation arithmetic and retained-expression valuation dispatch out of `ExactLocalValue.valuation`. **Acceptance:** `_rational_padic_valuation` owns exact rational arithmetic, `_expression_valuation` owns expression-tree cases, and the public method is below the repository complexity threshold without changing three-valued valuation semantics | `acceptance` |
| `refactor-abelian-tensor-unitors` | **Closed.** Move tensor-unit scalar arithmetic and left/right unitor component dispatch out of the `AbelianTensor` constructor. **Acceptance:** `_tensor_scalar`, `_tensor_unitor_inverse`, and `_tensor_unitor_component` own those operations, while `AbelianTensor` only assembles the selected monoidal structure and is below the repository complexity threshold | `acceptance` |
| `refactor-abelian-associator-dispatch` | **Closed.** Decompose tensor associator dispatch into native-presented, indexed-free, and finite-coordinate owners. **Acceptance:** `_presented_rebracket`, `_indexed_rebracket`, and the directional `_coordinate_rebracket_*` helpers own the three realizations, while `_rebracket` only selects among them and all associator helpers are below the repository complexity threshold | `acceptance` |
| `refactor-audit` | **Closed.** The repository-wide refactor audit is fully exploded into named DAG nodes and every recorded finding has been carried to its owner and closed; the scheduling node therefore has no remaining implementation work. **Acceptance:** all prerequisite refactor nodes and `acceptance` are closed, and `just plan-state` confirms the graph remains acyclic with resolved prerequisite IDs and a path from every required node to the terminal obligations | `acceptance`, `refactor-native-parent-membership`, `refactor-axiom-declaration-lookup`, `refactor-presented-native-presence`, `refactor-presented-coordinate-access`, `refactor-cardinality-declaration-probe`, `refactor-compiler-state-mutation`, `refactor-kernel-initializer-state`, `refactor-julia-cache-root`, `refactor-stub-generator-state`, `refactor-adele-open-state`, `refactor-set-symbolic-state`, `refactor-local-field-integral-state`, `refactor-construction-context-bookkeeping`, `refactor-stub-expression-names`, `refactor-stub-function-annotations`, `refactor-discrete-product-coproduct-scaffold`, `refactor-discrete-sequence-wrappers`, `refactor-stub-category-role-projection`, `refactor-cw-open-inclusion`, `refactor-thin-composition`, `refactor-gap-package-loading`, `refactor-construction-membership`, `refactor-adele-open-membership`, `refactor-local-field-valuation`, `refactor-abelian-tensor-unitors`, `refactor-abelian-associator-dispatch` |
| `type-paydown` | **Closed.** The post-refactor typing pass fixes source-local annotations only where they sharpen the actual runtime contract rather than encode checker workarounds. `_fullname` now states that its input is a class, matching its use of `__module__`/`__qualname__`; the exact Sage profile accepts that boundary. Remaining project diagnostics are category-plugin/dependency-stub projection gaps or would require source contortions that reduce legibility, so they are outside this node's stated standard. **Acceptance:** `MYPYPATH=src .venv/bin/mypy --config-file ~/ai-review-ci/tool-configs/mypy-sage.ini src/sage_categories/cat_kernel/static_projection.py` passes | `refactor-audit` |
| `bloat-finite-diagram-lowering` | **Closed.** Consolidate the duplicated FinSetsForCAP lowering shared by finite category limits and colimits. **Acceptance:** `_lower_finite_diagram` owns vertex/value indexing, native factors, and decorated arrows; `compatible_families` and `identified_objects` retain only the directional CAP operation and readback | `type-paydown` |
| `bloat-point-member-alias` | **Closed.** Keep one state-reading owner for the sole object of a point category. **Acceptance:** `PointCategory.member()` reads `_member`, while the callable convenience delegates to that public accessor instead of duplicating the retained-state read | `type-paydown` |
| `bloat-magma-inherited-accessors` | **Closed; audit finding rejected after a current-head public-consumer counterexample.** Category-class inheritance does not carry another category's role declarations: `POL-CAT-053`/`POL-CAT-057` require `MagmaCategory` to write its own object and morphism role mathematics, while executable implementation inheritance comes from selected structure functors. Deleting the local `carrier()`, `structure()`, and `underlying_morphism()` declarations made a constructed magma lack `carrier()` and broke ring construction before the RingedSpaces consumer could run. **Acceptance:** the three Magma-local declarations are restored, and `tests/algebra/test_magma_scaffold.sage::test_left_zero_magma_and_nonidentity_homomorphism` proves the carrier, structure/operation, nonidentity underlying morphism, and forgetful images through the public owner | `type-paydown` |
| `bloat-topological-space-projection` | **Closed.** Consolidate the identical forgetful functor from structured topological objects to `TopologicalSpaces()`. **Acceptance:** `_topological_space_projection` is the sole implementation of the shared `space()`/`continuous_map()` projection and both ringed spaces and topological rings delegate their named `to_spaces()` functors to it | `type-paydown` |
| `bloat-faithful-factor-projections` | **Closed.** Consolidate the repeated faithful-isofibration projection from retained limit families to a selected factor. **Acceptance:** `_faithful_isofibration_projection` owns the componentwise functor construction and the monoid-pair/action-pair named projections delegate to it without changing their public names or inherited-state semantics | `type-paydown` |
| `bloat-finite-power-data` | **Closed.** Consolidate binary-product and finite-power universal-data lookup over sequence diagrams. **Acceptance:** `_finite_power_data` owns sequence-diagram construction plus `constructed_data` lookup, while `binary_product_data` and `power_data` retain only their distinct finite-family spellings | `type-paydown` |
| `bloat-calculus-cone-imports` | **Closed.** Remove repeated local imports of cone constructors from `cat/calculus.py` now that the module already imports the cone owner at module scope. **Acceptance:** `cone`/`cones` are imported once beside `LimitConesCategory`, and product/terminal/power helpers contain no duplicate local cone imports | `type-paydown` |
| `bloat-adjunction-mor-imports` | **Closed.** Remove duplicate function-local imports of `Mor` from adjunction transpose/untranspose now that the module already depends on the morphism owner. **Acceptance:** `Mor` is imported once beside `MorphismCategory`, and both transposition directions use that shared binding without changing Hom admission | `type-paydown` |
| `bloat-finite-discrete-imports` | **Closed.** Remove repeated local imports of `DiscreteCategory` from finite-category evaluation now that `shapes.py` does not depend on the evaluator. **Acceptance:** `DiscreteCategory` is imported once at module scope and finite-object, dispatch, and finite-limit paths share the binding | `type-paydown` |
| `bloat-finite-sets-owner-import` | **Closed.** Remove eight delayed imports of the implemented Sets leaf from the FinSetsForCAP adapter and bind the owned `Sets` declaration once instead. **Acceptance:** `engines/finite_sets.py` imports `Sets` only from `cat.declarations`, all native reconstruction paths use that one owned declaration, and the engine no longer reaches back into `sets/finite.py` | `type-paydown` |
| `bloat-presented-module-owner` | **Closed.** Consolidate the repeated lazy lookup of the exact `Ab` owner in presented-module native retention. **Acceptance:** `_owner()` is the sole lazy `AbelianGroups` import at the retention boundary and both object/morphism native records use it | `type-paydown` |
| `bloat-oscar-runtime-modules` | **Closed.** Consolidate the repeated cycle-safe imports of the declaration, morphism, and structured-object owners at the OSCAR ring reconstruction boundary. **Acceptance:** `_ring_runtime_modules()` owns those three dynamic imports, and owner/object/morphism reconstruction share it while preserving delayed loading | `type-paydown` |
| `bloat-finite-category-limit-engine` | **Closed.** Consolidate the repeated lazy import of the finite-set category-limit adapter while preserving delayed engine loading. **Acceptance:** `_category_limits_engine()` is the sole import site for `engines.category_limits`, and Grothendieck plus general finite-limit evaluation share it | `type-paydown` |
| `bloat-finite-sets-category-evaluator` | **Closed.** Consolidate repeated lazy access to the finite-category evaluator in the FinSetsForCAP adapter while preserving the cycle-safe engine boundary. **Acceptance:** `_finite_category_data()` is the sole delayed import of `finite_category`, primitive limit/colimit and native-diagram lowering share it, and `Unknown` is bound once at module scope | `type-paydown` |
| `bloat-sets-finite-engine-boundary` | **Closed.** Consolidate repeated delayed imports of the FinSetsForCAP adapter from the Sets implementation. **Acceptance:** `_finite_sets_engine()` is the sole cycle-safe import of `engines.finite_sets` in `sets/finite.py`, and equality, mono/epi/inverse, universal-construction, factorization, Hom, and cartesian-comparison paths all use it | `type-paydown` |
| `bloat-sets-finite-category-boundary` | **Closed.** Consolidate repeated delayed imports of the finite-category evaluator from the Sets implementation. **Acceptance:** `_finite_category_engine()` is the sole cycle-safe import of `cat.finite_categories` in `sets/finite.py`, and product enumeration plus finite limit/colimit selection use that shared boundary | `type-paydown` |
| `bloat-functor-finite-category-boundary` | **Closed.** Consolidate repeated delayed finite-category lookup in extensional functor/transformation equality. **Acceptance:** `_finite_category_data()` is the sole cycle-safe import of `finite_category` in `cat/functors.py`, and both finite extensional equality handlers delegate to it | `type-paydown` |
| `bloat-diagram-constructed-data-boundary` | **Closed.** Consolidate repeated delayed universal-data lookup in pointwise diagram constructions. **Acceptance:** `_constructed_data()` is the sole cycle-safe import of `constructed_data` in `cat/diagrams.py`, and pointwise limit/colimit assembly both use it | `type-paydown` |
| `bloat-canonical-fp-engine-boundary` | **Closed.** Consolidate repeated delayed FpCategories imports in the canonical finitely presented category owner. **Acceptance:** `_fp_categories_engine()` is the sole cycle-safe import of `engines.fp_categories` in `cat/canonical.py`, and finite Hom enumeration, terminal selection, reduction, isomorphism detection, composition, and inversion all use it | `type-paydown` |
| `bloat-construction-full-image-boundary` | **Closed.** Consolidate repeated delayed full-image registration for chosen limit and colimit functors. **Acceptance:** `_register_construction_full_image()` is the sole cycle-safe import of `register_full_image` in `cat/constructions.py`, and both chosen-construction functors delegate to it | `type-paydown` |
| `bloat-kan-adjunction-runtime` | **Closed.** Consolidate the repeated cycle-safe runtime helper bundle shared by left/right Kan adjunction constructors. **Acceptance:** `_kan_adjunction_runtime()` is the sole delayed import site for `precompose`, `comma_objects`, and `Mor` in `cat/kan.py`, and both adjunction directions use it | `type-paydown` |
| `bloat-morphism-engine-boundaries` | **Closed.** Consolidate repeated delayed native cell/equation imports in the morphism owner. **Acceptance:** `_cells_engine()` and `_equations_engine()` are the sole cycle-safe imports of those adapters in `cat/morphisms.py`, and dimension, boundary, typecheck, equality, and reduced-word operations all use them | `type-paydown` |
| `bloat-cone-terminal-boundary` | **Closed.** Consolidate repeated delayed `Cat().Terminal()` construction in cone objects/morphisms. **Acceptance:** `_terminal_category_and_star()` is the sole cycle-safe import of `Cat` in `cat/cones.py`, and both cone construction paths share the retained terminal category/object | `type-paydown` |
| `bloat-finite-sets-cone-boundaries` | **Closed.** Consolidate repeated delayed cone/cocone imports in the FinSetsForCAP adapter. **Acceptance:** `_limit_cone_runtime()` and `_colimit_cocone_runtime()` are the sole cycle-safe imports of those public cone constructors in `engines/finite_sets.py`, and finite limit/product/equalizer plus colimit/coproduct/coequalizer all use them | `type-paydown` |
| `bloat-property-pullback-runtime` | **Closed.** Consolidate the repeated delayed pullback-construction helper bundle used by property inverse images and intersections. **Acceptance:** `_subcategory_pullback_runtime()` is the sole cycle-safe import site for `cone`, `cone_apex`, `cospan_diagram`, `Cat`, and `Fun` in `cat/properties.py`, and both pullback paths use it | `type-paydown` |
| `bloat-weighted-refinement-import` | **Closed.** Remove duplicate function-local imports of kernel refinement from the Yoneda helpers. **Acceptance:** `refine` is imported once at module scope in `cat/weighted.py`, and both `yoneda` and `coyoneda` use that shared binding without changing their fully-faithful refinement | `type-paydown` |
| `bloat-universal-arrow-adjunction-boundary` | **Closed.** Consolidate repeated delayed `Adjunctions` imports in right/left universal-arrow construction. **Acceptance:** `_adjunctions()` is the sole cycle-safe import of `Adjunctions` in `cat/universal_arrows.py`, and both adjunction directions use it | `type-paydown` |
| `bloat-predicate-semantic-bases-boundary` | **Closed.** Consolidate repeated delayed compiler semantic-ancestry lookup in predicate dispatch. **Acceptance:** `_semantic_bases()` is the sole cycle-safe import of `runtime_semantic_bases` in `kernel/predicates.py`, and both atom-type construction and argument-domain matching use it | `type-paydown` |
| `bloat-functor-declaration-boundary` | **Closed.** Consolidate repeated delayed functor-category lookup in the kernel declaration readers. **Acceptance:** `_functors()` is the sole cycle-safe import of `Fun` in `cat_kernel/functor_declarations.py`, and placement, inheritance, and point-declaration readers all use it | `type-paydown` |
| `bloat-cell-morphism-imports` | **Closed.** Remove redundant local imports of the already module-bound `MorphismCategory` in the homotopy-cell adapter. **Acceptance:** `engines/cells.py` has one module-level binding, and root-owner traversal, native-object lowering, and boundary reconstruction all use it | `type-paydown` |
| `bloat-presented-group-reconstruction-boundary` | **Closed.** Consolidate repeated delayed additive object reconstruction in the CAP presented-module adapter. **Acceptance:** `_owned_group_from_engine()` is the sole cycle-safe import of `_group_from_engine` in `engines/presented_modules.py`, and both cokernel and tensor-product native objects reconstruct through it | `type-paydown` |
| `bloat-additive-colift-boundary` | **Closed.** Consolidate repeated delayed CAP epimorphism-colift imports in relative tensor operations. **Acceptance:** `_colift_presented_epimorphism()` is the sole cycle-safe import of `colift_along_epimorphism` in `algebra/abelian.py`, and induced left/right actions plus relative associator descent all use it | `type-paydown` |
| `bloat-oscar-reconstruction-boundary` | **Closed.** Consolidate repeated delayed OSCAR execution-adapter imports in commutative-ring reconstruction. **Acceptance:** `_oscar_runtime()` is the sole cycle-safe import of `engines.oscar` in `_commutative_rings_oscar.py`, and native object/morphism reconstruction both use it | `type-paydown` |
| `bloat-category-finite-evaluator-boundary` | **Closed.** Consolidate repeated delayed finite-category evaluation in generic morphism enumeration. **Acceptance:** `_finite_category_data()` is the sole cycle-safe import of `finite_category` in `cat/category.py`, and both `morphisms()` and `hom_morphisms()` use it after their primary exact query path | `type-paydown` |
| `bloat-category-functor-boundary` | **Closed.** Consolidate the repeated delayed functor-category import throughout category-core execution. **Acceptance:** `_functors()` is the sole bootstrap import of `Fun` in `cat/category.py`, and declaration queries, exponentials, point/arrow functors, restriction/lift registration, identities, and composition all use it | `type-paydown` |
| `bloat-category-native-engine-boundaries` | **Closed.** Consolidate repeated delayed Catlab and homotopy-cell engine imports throughout category-core execution. **Acceptance:** `_catlab_engine()` and `_cells_engine()` are the sole category-core imports of those adapters, and functor/image/transformation execution plus cell identity/inverse/composite retention all use them | `type-paydown` |
| `bloat-category-canonical-boundary` | **Closed.** Consolidate repeated delayed imports of the canonical finite-category constructors in `Cat()`. **Acceptance:** `_canonical_categories()` is the sole bootstrap import of `cat.canonical` in `cat/category.py`, and presented/initial/simplex/boundary/horn/walking constructors all use it | `type-paydown` |
| `bloat-category-refine-reimports` | **Closed.** Remove redundant local imports of the already module-bound kernel `refine` operation from category-core methods. **Acceptance:** `cat/category.py` has one module-level `refine` binding and identities, inverses, morphisms, 2-cells, functor identities, and functor composition all use it | `type-paydown` |
| `bloat-finite-category-dispatch` | **Closed.** Separate finite-category dispatch that is available during bootstrap from the category kinds whose declarations must remain delayed. **Acceptance:** `_evaluate` handles only bootstrap-stable category classes, `_evaluate_runtime_category` owns the delayed Grothendieck/slice imports, and both dispatchers are below the repository complexity threshold | `type-paydown` |
| `bloat-predicate-global-assumptions` | **Closed.** Remove repeated local imports of SymPy's global assumption context from predicate helpers. **Acceptance:** `global_assumptions` is bound once at module scope and `unconditional`, `assume`, and `retract` share it | `type-paydown` |
| `bloat-category-discrete-shape-boundary` | **Closed.** Consolidate repeated delayed access to the `Discrete` shape family in functor limit lifting. **Acceptance:** `_discrete_shape_family()` is the sole bootstrap import of `Discrete` in `cat/category.py`, and limit-lifting registration and lookup both use it | `type-paydown` |
| `bloat-stubgen-runtime-boundary` | **Closed.** Consolidate the repeated delayed import of mypy's stub generator across public and private static-projection passes. **Acceptance:** `_stubgen_main()` is the sole `mypy.stubgen.main` import in `kernel/stub_generator.py`, and both projection passes invoke it without importing mypy during package bootstrap | `type-paydown` |
| `bloat-monoidal-discrete-morphism` | **Closed.** Consolidate the identical identity-only morphism rule of the discrete categories of supplied monoidal structures and actions. **Acceptance:** `_identity_only_morphism()` owns endpoint identity admission plus arrow construction, and both category implementations delegate to it | `type-paydown` |
| `bloat-additive-cone-accessors` | **Closed.** Remove redundant local imports of cone/cocone apex accessors from additive universal-map construction. **Acceptance:** `cone_apex` and `cocone_apex` are bound once with the module's existing cone imports, and indexed coproduct plus biproduct mediator paths share those bindings | `type-paydown` |
| `bloat-functor-diagram-boundary` | **Closed.** Consolidate repeated delayed diagram execution imports in the functor-category owner. **Acceptance:** `_diagrams()` is the sole cycle-safe import of `cat.diagrams` in `cat/functors.py`, and evaluation, constant/diagonal/object-rule diagrams, commuting-square data, and pointwise limits all use it | `type-paydown` |
| `bloat-property-functor-bootstrap` | **Closed.** Consolidate repeated delayed `Cat`/`Fun` lookups in property pullback execution. **Acceptance:** `_categories()` and `_functors()` are the sole property-layer bootstrap imports from `cat.functors`, and inverse-image keys plus pullback runtime construction share those owners | `type-paydown` |
| `bloat-kan-universal-arrow-boundary` | **Closed.** Consolidate the repeated delayed universal-arrow module import across left and right Kan adjunction construction. **Acceptance:** `_universal_arrows()` is the sole Kan-layer import of `cat.universal_arrows`, and both adjunction directions select their directional initial/terminal and universal-arrow constructors from it | `type-paydown` |
| `bloat-weighted-calculus-boundary` | **Closed.** Consolidate the repeated delayed calculus import used by Yoneda and co-Yoneda construction. **Acceptance:** `_calculus()` is the sole weighted-layer import of `cat.calculus`, and both embeddings obtain curry/transpose through that shared boundary | `type-paydown` |
| `bloat-predicate-property-boundary` | **Closed.** Consolidate the repeated delayed property-runtime import used by axiom construction and transport. **Acceptance:** `_properties()` is the sole predicate-layer import of `cat.properties`, and default property-subcategory construction plus inherited inverse-image retention use it | `type-paydown` |
| `bloat-cone-opposite-boundary` | **Closed.** Consolidate repeated delayed opposite-category imports in cone utilities. **Acceptance:** `_opposites()` is the sole cone-layer import of `cat.opposites`, and cocone arrow reversal plus opposite-shape vertex resolution use it | `type-paydown` |
| `bloat-indexed-module-additive-boundary` | **Closed.** Consolidate integer-module presentation access to the additive owner and remove the stale deleted presentation accessor. **Acceptance:** all required additive constructors/maps/coordinate conversion are module-level imports, matrix and basis conversion use `_coordinates()`, and `indexed_modules.py` contains no function-local `algebra.abelian` import | `type-paydown` |
| `bloat-presented-module-additive-boundary` | **Closed.** Consolidate the repeated delayed additive-owner imports in the CAP presented-module adapter. **Acceptance:** `_abelian_owner()` is the sole adapter import of `algebra.abelian`, and coordinate lookup, object reconstruction, and native-morphism reconstruction all use it | `type-paydown` |
| `bloat-additive-presented-module-boundary` | **Closed.** Consolidate every delayed CAP presented-module import in the additive owner. **Acceptance:** `_presented_modules()` is the sole `algebra/abelian.py` import of the adapter, and equality, Hom/zero/biproduct/coequalizer/tensor/comparison/colift operations all invoke it through that boundary | `type-paydown` |
| `bloat-topological-ring-identity-owner` | **Closed.** Restore the explicit topology owner used by topological-ring identity construction. **Acceptance:** `TopologicalSpaces` is imported beside the existing topology category/projection bindings, `TopologicalRingsCategory.construct_identity` resolves both ring and topology identities from their exact owners, and the module is Ruff-clean | `type-paydown` |
| `bloat-role-kernel-boundaries` | **Closed.** Consolidate repeated delayed construction/compiler imports in the stable kernel role bases. **Acceptance:** `_construction()` and `_compiler()` are the sole role-layer imports of those cyclic runtime modules, and identity/placement/role lookup plus category construction/compile/recompile/source lookup all use them | `type-paydown` |
| `bloat-axiom-predicate-boundary` | **Closed.** Consolidate repeated delayed public-predicate imports in kernel axiom installation. **Acceptance:** `_predicates()` is the sole `cat_kernel/axioms.py` import of `cat.predicates`, and subclass axiom recognition plus generated application lookup use it | `type-paydown` |
| `bloat-compiler-refinement-boundary` | **Closed.** Consolidate repeated delayed refinement-graph imports in the category compiler. **Acceptance:** `_refinement()` is the sole compiler import of `kernel.refinement`, and inheritance tracing plus selected-functor placement/point checks use it | `type-paydown` |
| `bloat-compiler-role-reimports` | **Closed.** Remove redundant local imports from the already module-bound kernel role owner. **Acceptance:** `category_universal_class`, `declared_roles`, and `role_of` join the existing compiler role imports, and no function-local `kernel.roles` import remains | `type-paydown` |
| `bloat-finite-category-slice-boundary` | **Closed.** Consolidate repeated delayed slice-category imports in finite-category evaluation. **Acceptance:** `_slices()` is the sole evaluator import of `cat.slices`, and runtime slice recognition plus comma endpoint/pair construction use it | `type-paydown` |
| `bloat-finite-sets-cone-module` | **Closed.** Collapse the parallel limit/cocone loaders in the FinSetsForCAP adapter to one delayed cone-module owner. **Acceptance:** `_cones()` is the sole `finite_sets.py` import of `cat.cones`, and finite limit/colimit/product/equalizer/coproduct/coequalizer execution selects cone/cocone operations from it | `type-paydown` |
| `bloat-property-cat-derivation` | **Closed.** Remove the separate `Cat` bootstrap import from property execution and recover it from `Fun = Mor(Cat)`. **Acceptance:** `_categories()` returns `_functors().base_category()`, inverse-image keys and pullback/intersection diagrams use that retained category object directly, and `cat.properties` imports only `Fun` from `cat.functors` | `type-paydown` |
| `bloat-property-axiom-declaration-probe` | **Closed.** Replace dynamic attribute probing when a property subcategory registers its declared axiom implementation. **Acceptance:** `PropertySubcategory.__init_subclass__` reads the exact `vars(declaring_class)` namespace once, requires the named axiom there, and uses no `hasattr`/`getattr` lookup | `type-paydown` |
| `bloat-adjunction-inhabitation` | **Closed.** Consolidate the identical retained-object inhabitation rule of adjunction and equivalence data categories. **Acceptance:** `_retained_inhabitation()` owns the retention decision and both `_chosen_inhabitation` methods delegate to it | `type-paydown` |
| `bloat-universal-diagram-category` | **Closed.** Consolidate the repeated `Fun(shape, ambient)` construction used by limit/colimit and discrete product/coproduct families. **Acceptance:** `_diagram_category()` owns general diagram-category construction, both shape-indexed universal families use it, and `_discrete_diagrams()` adds only its discrete-shape admission check | `type-paydown` |
| `bloat-dual-functor-round-trip` | **Closed.** Consolidate repeated identity-round-trip natural-isomorphism construction in functor-category duality. **Acceptance:** `_identity_round_trip()` owns endofunctor identities, forward/inverse transformations and retained inverse pairing; the duality unit/counit differ only by their component rule | `type-paydown` |
| `bloat-projective-infinity-maps` | **Closed.** Split finite-stage legs, weak-open preimages, and cocone descent out of the projective-infinity constructor. **Acceptance:** the three universal-map responsibilities have named helpers and `projective_infinity()` retains only object/topology construction plus final colimit wiring | `type-paydown` |
| `bloat-projective-line-sheaf` | **Closed.** Split the projective-line finite-cover sheaf presentation out of the chart-gluing constructor. **Acceptance:** `_projective_line_structure_sheaf()` owns cover/sections/restrictions/presheaf reconstruction, while `projective_line()` retains chart, overlap, gluing, swap and final presentation assembly | `type-paydown` |
| `bloat-adele-algebra-topology` | **Closed.** Separate exact adelic ring construction from restricted-product topology/continuity installation. **Acceptance:** `_adele_ring()` owns carrier/zero/one/ring operations, `_adele_topological_ring()` owns opens and binary-operation continuity, and `adeles_of_rationals()` only assembles the final presentation | `type-paydown` |
| `bloat-semiring-law-equations` | **Closed.** Separate semiring law equations from the public semiring-category constructor. **Acceptance:** `_semiring_law_equations()` owns left/right distributivity and both zero-absorption natural-transformation pairs, while `Semirings()` assembles the monoid-pair pullback and applies those equations through equifiers | `type-paydown` |
| `bloat-ring-factor-projections` | **Closed.** Reuse the shared faithful-isofibration projection owner for the two ring pullback legs. **Acceptance:** `RingCategory.to_semiring()` and `to_additive_group()` delegate to `_faithful_isofibration_projection` with their exact factor index and contain no duplicate projection construction | `type-paydown` |
| `bloat-profunctor-unitor-components` | **Closed.** Separate co-Yoneda descent from its inverse identity-arrow injection. **Acceptance:** `_unitor_descent_component()` owns the profunctor transport rule, `_unitor_inverse_component()` owns the weighted injection, and `_unitor_components()` only constructs the two maps and retains them as inverses | `type-paydown` |
| `bloat-matching-triples-native` | **Closed.** Separate owned-value indexing from the CAP equalizer selecting finite matching triples. **Acceptance:** `_matching_native_selection()` owns the skeletal product/projections/maps/equalizer and returns selected native indices, while `matching_triples()` converts owned values to/from those indices | `type-paydown` |
| `bloat-finite-limit-reconstruction` | **Closed.** Consolidate owned reconstruction of finite limit objects/morphisms after native component computation. **Acceptance:** `_reconstruct_limit_family()` owns object construction, component-identity indexing and exact morphism reconstruction; both product and general finite-limit branches delegate to it | `type-paydown` |
| `bloat-bimodule-unitors` | **Closed.** Consolidate the mirrored left/right relative-tensor unitor component scaffolds for bimodules. **Acceptance:** `_bimodule_unitor_components()` owns side-specific source/projection/unitor selection plus bimodule wrapping, and one cached `unitor_components(value, side)` feeds both unit natural isomorphisms | `type-paydown` |
| `bloat-bimodule-associator` | **Closed.** Separate relative-tensor associator descent from bimodule monoidal-structure assembly. **Acceptance:** `_bimodule_associator_components()` owns both quotient descents, inverse equations and bimodule wrapping; `AbelianBimoduleTensor()` retains only a cached delegating family plus the associator natural-isomorphism wiring | `type-paydown` |
| `bloat-free-associative-multiplication` | **Closed.** Separate native free-algebra multiplication from free-associative object assembly. **Acceptance:** `_free_associative_multiplication()` owns coefficient conversion, native multiplication, relative-tensor mediation and bimodule wrapping; `integer_free_associative_algebra()` retains carrier/action/unit/object construction | `type-paydown` |
| `bloat-projective-line-swap` | **Closed.** Separate the projective-line chart-swap automorphism from cover/gluing construction. **Acceptance:** `_projective_line_swap()` owns both chart pullbacks, the gluing mediator and induced overlap localization automorphism; `projective_line()` consumes the returned pair during final presentation assembly | `type-paydown` |
| `bloat-free-associative-substitution-layers` | **Closed.** Separate generator-image linearization from lifting a substitution through bimodule/monoid owners. **Acceptance:** `_free_associative_linear_substitution()` owns the indexed-module endomorphism, `_free_associative_algebra_substitution()` owns bimodule/monoid lifting, and the public substitution function validates images then composes them | `type-paydown` |
| `bloat-presented-colimit-phases` | **Closed.** Split finite presented-colimit lowering into named factor, object-quotient, and quotient-presentation phases. **Acceptance:** `_factor_presentations`, `_quotient_object_classes`, `_vertex_class`, and `_presented_colimit_category` own those transformations; `presented_colimit_in_opposite()` only assembles the retained injections, representatives, cone, and mediator | `type-paydown` |
| `bloat-predicate-owned-atom-import` | **Closed.** Remove the redundant function-local import of the kernel-owned SymPy atom constructor from applied queries. **Acceptance:** `_owned_atom` is bound once with the rest of `kernel.predicates`, and `AppliedQuery._sympy_()` uses that module-level boundary without changing proposition conversion | `type-paydown` |
| `bloat-shape-point-object-data` | **Closed.** Consolidate the identical retained-point data records used by discrete and thin shape objects. **Acceptance:** `_PointObjectData` is the sole private record carrying a shape-object point, and both `DiscreteCategory.ObjectType` and `ThinCategory.ObjectType` construct from it without changing their distinct admission or morphism semantics | `type-paydown` |
| `bloat-shape-point-object-surface` | **Closed.** Consolidate the identical retained-point object surface used by discrete and thin shapes. **Acceptance:** `_PointShapeObject` owns point initialization, access, and representation; both shape object roles inherit that surface while retaining distinct category admission and morphism semantics | `bloat-shape-point-object-data` |
| `bloat-shape-point-navigation` | **Closed.** Consolidate the identical point/object conversion and identity construction used by discrete and thin shapes. **Acceptance:** `_point_shape_object_at`, `_point_shape_object_point`, and `_point_shape_identity` own those three point-indexed operations, while each public shape category retains only its typed wrappers and distinct morphism admission/equality | `bloat-shape-point-object-surface` |
| `bloat-construction-shape-accessors` | **Closed.** Consolidate identical retained-shape and diagram-category access in chosen limit and colimit families. **Acceptance:** `_construction_shape` and `_construction_diagrams` are the shared owners, while `LimitsCategory` and `ColimitsCategory` keep only their directional universal-construction logic | `type-paydown` |
| `bloat-discrete-family-registry` | **Closed.** Consolidate the mirrored retained-family registry used by product and coproduct unions. **Acceptance:** `_retain_discrete_family`, `_discrete_full_images`, and `_presenting_discrete_family` own identity retention, nontrivial-family filtering, and unique presentation lookup; product/coproduct categories retain only their directional universal logic | `type-paydown` |
| `bloat-discrete-construction-input` | **Closed.** Consolidate product/coproduct normalization of tuple inputs into a nontrivial discrete diagram. **Acceptance:** `_discrete_construction_diagram` owns sequence conversion plus nontrivial-shape admission, while product/coproduct `__call__` methods retain only their directional limit/colimit execution and codomain checks | `bloat-discrete-family-registry` |
| `bloat-fp-presentation-identity-cache` | **Closed.** Replace the hand-rolled `id()`-keyed presented-category cache with Sage identity storage. **Acceptance:** `engines/fp_categories.py` uses one `MonoDict` keyed by the owned category itself, with no parallel integer-key/owner-assertion protocol, while retaining one native presentation per owned category identity | `type-paydown` |
| `bloat-catlab-recipe-identity-cache` | **Closed.** Replace hand-rolled `id()` recipe tables at the Catlab boundary with Sage identity storage. **Acceptance:** functor and transformation recipes are keyed directly by their owned morphisms in `MonoDict`, with no integer-key/retained-owner tuples; replacement semantics for unmaterialized transformations remain unchanged | `type-paydown` |
| `bloat-cokernel-difference-identity-cache` | **Closed.** Replace the hand-rolled `id()` table retaining CAP cokernel differences with Sage identity storage. **Acceptance:** `_cokernel_differences` is a `MonoDict` keyed directly by the owned projection, and mediator lookup contains no integer key, duplicate retained owner, or manual identity assertion | `type-paydown` |
| `bloat-cell-identity-caches` | **Closed.** Replace the homotopy-cell adapter's hand-rolled `id()` registries with Sage identity storage. **Acceptance:** category states, cell owners, native objects, and native morphisms are keyed directly by their owned values in `MonoDict`; no duplicate owner tuples or manual id-reuse assertions remain | `type-paydown` |
| `publish-for-consumers` | **Closed 2026-09-17 at published source snapshot `2c0c97eeaa2b5cfb21762ab80bd03e279d8d832d`.** The owner-authorized consolidation incorporates all 138 inherited staged/unstaged/untracked paths without changing their contents, fast-forwards the integrated history onto `main`, and publishes it to `origin/main`. Live `git ls-remote` confirmed the exact source SHA; the integrated history and `research` pin `66efc15bf5050a527f1bb4ff3bff8542e3d83203` are reachable from that published ref. The ordinary push passed plan-state, rule coverage, all seven architecture contracts, and doctor preflight before stopping solely at `_sage-preflight` because `SAGE_BIN` is unset. The owner's explicit source-publication instruction was carried through with `--no-verify`; hooks are unchanged. This publishes the source snapshot under `README.md`'s consumer contract, not runtime acceptance: `completion-runtime-beta10`, full static integration, and integrated behavioral acceptance remain open. | none |
| `bloat-inverse-image-retention-phases` | **Closed.** Separate inverse-image pullback factorization from propagation through declared target containments. **Acceptance:** `_inverse_image_mediator()` owns refinement of a candidate cone into the retained realization, `_retain_inverse_image_comparisons()` owns comparison pullbacks along target containments, and `retain_inverse_image()` only installs the chosen pullback and invokes those phases | `type-paydown` |
| `bloat-limit-basis-phases` | **Closed.** Separate limit-basis equation construction, vertex indexing, and candidate factorization. **Acceptance:** `_basis_parallel_maps()`, `_basis_index()`, and `_basis_lift()` own those phases and `_basis_data()` only assembles the selected product/equalizer universal presentation | `type-paydown` |
| `bloat-role-typeis-import` | **Closed.** Remove the runtime import of the type-only `TypeIs` helper from the kernel role boundary. **Acceptance:** `TypeIs` is imported only under `TYPE_CHECKING`, while `is_category()` retains its static narrowing annotation under postponed annotations | `type-paydown` |
| `bloat-ring-presheaf-validation` | **Closed.** Separate finite ring-presheaf law validation from contravariant functor assembly. **Acceptance:** `_validate_ring_presheaf_data()` owns section-ring ownership, restriction endpoints, identities, and composition checks; `ring_presheaf()` only assembles the validated section functor and presentation | `type-paydown` |
| `bloat-scheme-affine-wrapper-identity-cache` | **Closed, superseded at the mechanism level.** The first repair replaced the `id()` dictionary with `MonoDict`; `evaluate-unique-representation` removed the memoization protocol itself. **Acceptance:** `SchemesCategory.affine()` is a Sage `cached_method`, `_affine_wrappers` is gone, and the affine wrapper still retains the exact OSCAR covered scheme | `type-paydown` |
| `bloat-discopy-wire-identity-cache` | **Closed.** Replace the DisCoPy semantic model's hand-rolled `id()` wire-token table with Sage identity storage. **Acceptance:** `_wire_tokens` is a `MonoDict` keyed directly by the owned object, while stable wire-token naming and reverse token-to-value reconstruction remain unchanged | `type-paydown` |
| `bloat-gap-local-identity-maps` | **Closed.** Replace local raw-`id()` object-to-native maps in the slice and functor-category GAP adapters with Sage identity storage. **Acceptance:** both adapters use `MonoDict` keyed directly by owned objects/arrows during native reconstruction, with no integer identity keys while preserving the same native cells and transformations | `type-paydown` |
| `bloat-discopy-word-identity-key` | **Closed.** Replace the DisCoPy semantic model's raw tuple of object ids for word caching with the repository identity-key helper. **Acceptance:** `_word()` keys `_word_cache` with `identity_key(*word)`, retaining each owned object against id reuse while preserving the same word-order cache semantics | `type-paydown` |
| `bloat-discopy-path-object-cache` | **Closed.** Replace the path evaluator's local raw-`id()` object cache with Sage identity storage. **Acceptance:** `evaluate_path()` uses a `MonoDict` keyed directly by owned objects for its DisCoPy semantic wrappers, while the external string tokens used by DisCoPy remain unchanged | `type-paydown` |
| `bloat-oscar-certified-ring-reconstruction` | **Closed.** Remove the second commutative-ring certification implementation from the OSCAR reconstruction adapter. **Acceptance:** `reconstruct_oscar_object()` constructs only the OSCAR-backed carrier and its four ring rules/constants, then delegates the additive-group, multiplicative-monoid, semiring, ring, and commutativity refinement chain to `certified_commutative_ring()` | `type-paydown` |
| `bloat-indexed-cache-identity-keys` | **Closed.** Replace bare-`id()` Sage cache keys throughout indexed-category execution with the repository identity-key contract. **Acceptance:** object/reindexing/unit/compositor, transformation component/comparison, strictification, and fiber-equivalence caches all use `identity_key(...)`, so cached inputs are retained against Python id reuse without invoking proposition-valued equality | `type-paydown` |
| `bloat-theory-cache-identity-keys` | **Closed.** Route hand-written retained identity cache keys in category theory modules through the repository identity-key owner. **Acceptance:** sequence diagrams, functor restriction, category-product sequence construction, and property-subcategory candidate caches use `identity_key(...)` rather than spelling `(id(value), value)` locally | `type-paydown` |
| `bloat-predicate-atom-value-map` | **Closed.** Remove the predicate kernel's parallel integer-id lookup table for recovering owned values from private SymPy atoms. **Acceptance:** every `_OwnedValueAtom` directly keys its retained owned value in `_atom_values`; atom creation may still encode Python identity in the private SymPy payload, but no separate `id(value) -> value` registry remains | `type-paydown` |
| `bloat-represented-product-position-map` | **Closed.** Replace the represented Sets product's raw-`id()` vertex-position table with Sage identity storage. **Acceptance:** `_represented_product` indexes its discrete vertices directly through `MonoDict`, preserving exact projection positions without carrying bare Python identities | `type-paydown` |
| `bloat-finite-set-vertex-position-maps` | **Closed.** Replace FinSetsForCAP's repeated raw-`id()` vertex-position tables with Sage identity storage. **Acceptance:** `_identity_positions` owns identity-safe vertex indexing and native finite diagrams, products, and coproducts index retained vertices directly through `MonoDict` | `type-paydown` |
| `bloat-category-limit-position-maps` | **Closed.** Replace finite category-limit lowering's raw-`id()` vertex and fiber position maps with Sage identity storage. **Acceptance:** `_identity_positions` owns direct retained-value indexing and `_lower_finite_diagram` contains no bare Python identity keys while preserving its FinSetsForCAP graphs | `type-paydown` |
| `bloat-finite-category-identity-maps` | **Closed.** Replace raw Python-id maps in finite Grothendieck, limit, and comma reconstruction with the repository identity owners. **Acceptance:** fiber lookup uses `MonoDict`, composite reconstruction keys use `identity_key(...)`, and `cat/finite_categories.py` contains no raw `id(...)` lookup tables for retained owned values | `type-paydown` |
| `bloat-sheaf-gluing-phases` | **Closed.** Separate finite sheaf-family compatibility checks from verification of the selected global section. **Acceptance:** `_validate_gluing_family()` owns cover, section-parent, and overlap compatibility checks; `_verify_gluing_result()` owns restriction and uniqueness checks; `RingSheaf.glue()` only sequences validation, the supplied gluing rule, and result verification | `type-paydown` |
| `bloat-limit-basis-position-map` | **Closed.** Replace limit-basis presentation's raw-`id()` object-position table with Sage identity storage. **Acceptance:** `diagram_presentation()` indexes retained shape objects directly through `MonoDict` when constructing source/target generator diagrams, with no bare Python identity keys | `type-paydown` |
| `bloat-construction-retained-value-dedup` | **Closed.** Replace kernel retained-value deduplication through a bare-id set with Sage identity storage. **Acceptance:** `retained_values()` uses `MonoDict` membership keyed directly by the constructed value, preserving once-by-identity traversal without exposing Python ids | `type-paydown` |
| `bloat-predicate-retention-key` | **Closed.** Delete the property-axiom layer's duplicate identity-key constructor. **Acceptance:** property construction retention calls `kernel.retention.identity_key` directly and `cat/predicates.py` contains no second `(id(value), value)` key helper | `type-paydown` |
| `bloat-construction-order-identity-keys` | **Closed.** Replace construction-completion DAG nodes based on bare Python ids with the kernel's retained identity keys. **Acceptance:** `complete_constructions()` keys both declaration storage and `TopologicalSorter` dependencies with `identity_key(category)`, retaining each category that justifies an identity throughout recompilation ordering | `type-paydown` |
| `bloat-stub-ast-identity-ids` | **Closed.** Stop converting AST node identity to raw integers inside the static projector. **Acceptance:** projected alias tracking stores `ast.Assign` nodes directly, repeated declaration selection deduplicates hashable AST statements through `_selected_declarations`, and the projector contains no `id(statement)` bookkeeping | `type-paydown` |
| `bloat-discopy-path-token-ids` | **Closed.** Stop exposing Python memory identities as DisCoPy object-token names during path evaluation. **Acceptance:** `evaluate_path()` allocates local sequential object tokens through an identity-keyed `MonoDict` and reverse token map, while preserving source/target sharing and semantic readback | `type-paydown` |
| `bloat-local-field-rational-valuation` | **Closed.** Replace the local hand-rolled prime-divisibility loops for rational p-adic valuation with SymPy's mature multiplicity routine. **Acceptance:** `_rational_padic_valuation` delegates nonzero numerator/denominator exponents to `sympy.ntheory.multiplicity`, retaining the existing zero-to-infinity convention | `type-paydown` |
| `bloat-additive-integer-multiple` | **Closed.** Replace the local binary double-and-add implementation for integer multiples with Sage's mature generic group algorithm. **Acceptance:** `_integer_multiple` delegates positive, zero, and negative coefficients to `sage.groups.generic.multiple` with the owned additive identity/inverse/operation, and contains no local scalar-multiplication loop | `type-paydown` |
| `bloat-free-algebra-word-serialization` | **Closed.** Replace private FreeMonoid `_element_list` inspection with Sage's public word conversion. **Acceptance:** `_word()` reconstructs serialized generator positions from `native_word.to_list()` and the parent's public generators, with no private native representation access | `type-paydown` |
| `bloat-finite-set-public-boundary` | **Closed.** Remove FinSetsForCAP dependence on private set storage and map action fields. **Acceptance:** native finite object lowering reads `set_presentation()`, native and fallback universal-map evaluation goes through owned morphism application via `_owned_map_value`, and `engines/finite_sets.py` contains no `_values` or `_action` probes | `type-paydown` |
| `bloat-presented-category-public-boundary` | **Closed.** Remove native FpCategories/Catlab adapters' dependence on private generator/path endpoint tables. **Acceptance:** both adapters derive generator endpoints through public `generator()` arrows plus `label()`, derive relation endpoints from the public boundary generators, and contain no `_generator_endpoints` or `_path_endpoints` probes | `type-paydown` |
| `bloat-catlab-transformation-boundary` | **Closed.** Remove Catlab fallback materialization's direct read of a natural transformation's private assignment callback. **Acceptance:** `ensure_native_transformation()` hands the bridge the public `component` method when no retained recipe exists, preserving lazy component retention and eliminating `_assignment` probing from the engine adapter | `type-paydown` |
| `bloat-cell-invertibility-strengthening` | **Closed.** Consolidate duplicate native-cell invertibility strengthening and its homotopy-core composite-cell exception handling. **Acceptance:** `_strengthen_generator_invertibility` owns the generator-only strengthening boundary and both forward/backward inverse-retention paths delegate to it | `type-paydown` |
| `bloat-slice-dispatch-boundary` | **Closed.** Stop finite-category and GAP slice adapters from probing `SliceLikeCategory._fixed_label` directly. **Acceptance:** `_is_slice_over` in the slice owner encapsulates endpoint orientation for finite dispatch, `_varying_of` reuses it, and the native SliceCategories adapter trusts its already-filtered caller without reading private category state | `type-paydown` |
| `bloat-cw-open-datum-boundary` | **Closed.** Consolidate repeated cast chains that unwrap finite and weak CW opens from open-category objects. **Acceptance:** `_cw_open_datum` and `_weak_cw_open_datum` own the point/datum boundary and inverse-image constructions reuse them instead of repeating nested `Any` casts | `type-paydown` |
| `bloat-adele-binary-preimage-dispatch` | **Closed.** Replace string-dispatched adelic binary preimage evaluation with direct operation callables. **Acceptance:** `_binary_adele_preimage` owns the shared product-topology preimage construction, addition/multiplication pass their actual operations, and no internal string match selects ring arithmetic | `type-paydown` |
| `bloat-finite-partial-order-laws` | **Closed.** Replace the local cubic finite partial-order law checker with Sage's mature Poset closure/validation. **Acceptance:** `engines/order_relations.py` delegates finite order construction to `sage.combinat.posets.Poset`, compares Sage's completed relation with the supplied relation, and `order/posets.py` contains no hand-rolled reflexive/antisymmetric/transitive loops | `type-paydown` |
| `bloat-binary-relations-public-surface` | **Closed.** Stop BinaryRelations execution from bypassing its own retained carrier/map API. **Acceptance:** forgetful functor construction, relation-preservation checking, identities, and composition use `carrier()`, `underlying_map()`, and public set-map application; the owner contains no operational reads of `_carrier`, `_underlying_map`, or map `_action` outside their defining accessors | `type-paydown` |
| `bloat-local-field-rational-embedding-fallback` | **Closed.** Remove the silent zero fallback from exact rational embeddings into local fields. **Acceptance:** `ExactLocalFieldPresentation.embed_rational` requires the source value to have an exact rational payload and preserves that payload; a non-rational expression fails loudly instead of being replaced by zero | `type-paydown` |
| `bloat-runtime-reflection-probes` | **Closed.** Replace generic attribute-presence probes in category initialization and predicate-handler introspection with exact representation tests. **Acceptance:** category reinitialization checks the instance namespace for `_ordinal`, bound predicate handlers are recognized by `MethodType`, and neither path uses `hasattr` to infer runtime state | `type-paydown` |
| `bloat-role-declaration-owner-retention` | **Closed.** Consolidate duplicate declaration-owner insertion in the kernel role layer. **Acceptance:** `_retain_declaration_owner` owns first-writer retention explicitly and both subclass preparation and declaration validation delegate to it instead of repeating `setdefault` mutation | `type-paydown` |
| `bloat-binary-relations-forgetful-cache` | **Closed.** Replace the manual nullable cache for the BinaryRelations forgetful functor with Sage's method cache. **Acceptance:** `to_sets()` is a `cached_method`, `_forgetful` state and its conditional mutation are deleted, and the selected faithful/isofibration functor is still constructed once per category object | `type-paydown` |
| `bloat-indexed-structural-functor-caches` | **Closed.** Replace manual nullable caches for the Grothendieck construction functor and its projection with Sage method caching. **Acceptance:** `IndexedCategoriesCategory.grothendieck_functor()` and `GrothendieckCategory.projection()` are `cached_method`s, `_grothendieck_functor`/`_projection` state and first-call branches are deleted, and cartesian lifts are installed exactly once on the cached projection | `type-paydown` |
| `bloat-image-functor-caches` | **Closed.** Replace manual nullable factor/inclusion caches in image categories with Sage method caching. **Acceptance:** strict/full image factor and inclusion functors plus essential-image factorization are `cached_method`s, `_factor`/`_inclusion` fields and first-call branches are deleted, and each retained image functor is still constructed once per image owner | `type-paydown` |
| `bloat-slice-projection-caches` | **Closed.** Replace manual nullable caches for the two structural slice projections with Sage method caching. **Acceptance:** `defining_arrow()` and `fixed_projection()` are `cached_method`s, `_arrow_projection`/`_varying_projection` state and first-call branches are deleted, and `retain_lifts()` still installs lifts on the single cached varying projection | `type-paydown` |
| `bloat-universal-construction-functor-caches` | **Closed.** Replace manual nullable caches for universal-construction image factors, limit adjunctions, and colimit functors with Sage method caching. **Acceptance:** `_factor_through_image`, `adjunction`, and `colimit_functor` are `cached_method`s, `_image_factor`/`_limit_adjunction`/`_colimit_functor` state is deleted, and full-image/coproduct retention still occurs exactly when the cached functor is first constructed | `type-paydown` |
| `bloat-inverse-image-projection-cache` | **Closed.** Replace the inverse-image subcategory's manual nullable target-projection cache with Sage method caching. **Acceptance:** `target_projection()` is a `cached_method`, `_target_projection` state and its first-call branch are deleted, and pullback-derived monomorphism/isofibration refinement remains part of the cached construction | `type-paydown` |
| `evaluate-unique-representation` | **Closed.** Sage `CachedRepresentation`/`UniqueRepresentation` were checked against the actual kernel lifecycle before further identity-cache work. They do not replace construction retention: `CachedRepresentation` is a weak per-class cache keyed by constructor-argument equality/hash, while owned datums require identity when equality is proposition-valued and canonical objects must survive dynamic role-class recompilation; `UniqueRepresentation` additionally imposes equality-by-identity, contradicting category-owned mathematical `__eq__`. Root-input tables are post-construction provenance used by refinement/augmentation; compiler runtime tables hold replaceable metadata; functor-image tables cache actions on existing values; predicate atoms must be replaced when refinement changes a value's semantic class. These reasons are recorded locally and in `specs/resolution.md` under “Construction retention versus Sage representation caches.” The ordinary memoization case was removed: `SchemesCategory.affine()` is now a Sage `cached_method` and `_affine_wrappers` is gone. **Acceptance:** every kernel identity table is covered by the recorded lifecycle/key-semantics rationale, Sage `dynamic_class(..., cache=True)` remains the owner of runtime class identity, and affine wrapping contains no hand-written cache | none |
| `bloat-image-identity-override` | **Closed.** Delete the strict/full image layer's hand-written identity-table lifecycle without inventing placement that the image inclusion does not declare. An image identity is literally the target identity; strict/full image membership is retained separately because those inclusions are not generally isofibrations. **Acceptance:** `ImageCategory._identity_morphism_` uses Sage `cached_method` rather than `_identities` lookup/store, constructs through the image's `construct_identity`, retains the shared identity as its own inverse, preserves its target `Identity()` placement, and does not place it in the strict/full image morphism category | `evaluate-unique-representation` |
| `bloat-image-category-registry-cache` | **Closed.** Keep the strict/full image category identity mechanism because image categories may be requested before or after functor actions, and full images may be supplied by another universal-construction owner; remove the hand-written module registries by delegating that retained construction identity to Sage `cached_function` with `identity_key`. **Acceptance:** `_strict_images`/`_full_images` do not exist; `strict_image` and `full_image` are identity-keyed cached functions, `register_full_image` installs externally owned full images through the cache API, and pre/post image-action retention still detects only already-constructed images without constructing one as a side effect | `evaluate-unique-representation` |
| `bloat-point-functor-cache` | **Closed.** Keep one retained point functor per exact object, but remove the category-owned `_points` table and manual check/store protocol because this is an ordinary parameterized method cache once the required identity key is stated. **Acceptance:** `Category.point_functor` is `cached_method(key=identity_key)`, `_points` state is absent, point selection still works for an object under construction before placement, and repeated calls return the same functor without invoking proposition-valued object equality | `evaluate-unique-representation` |
| `bloat-cat-singleton-cache` | **Closed.** Remove the nullable module-global `_CAT` memoization protocol rather than improving its container. The semantic singleton still exists, but Sage's nullary cached-function owner stores it after the bootstrap constructs the self-referential category. **Acceptance:** `Cat()` is a `cached_function`, `bootstrap()` installs the one constructed value with `set_cache`, `_CAT` is absent, and ordinary import/bootstrap returns one stable `Cat()` object | `evaluate-unique-representation` |
| `bloat-enumeration-index-cache` | **Closed.** Keep the mathematically selected inclusion of a chosen enumeration's index set into `NN`, but remove the parallel `_enumeration_indices` registry and check/store protocol. **Acceptance:** `enumeration_index_inclusion()` is identity-keyed `cached_method`; supplied inclusions are installed with `set_cache`, conflicting re-registration still fails, and the module has no `_enumeration_indices` table | `evaluate-unique-representation` |
| `bloat-chosen-enumeration-cache` | **Closed.** Keep the selected enumeration itself as retained mathematical data, but remove the module-level `_enumerations` registry and manual positive-result cache lifecycle. **Acceptance:** `_retained_enumeration` is an identity-keyed Sage `cached_function` whose body is never a fallback constructor; finite/product reconstruction and supplied enumerations install positive results with `set_cache`, `Unknown` remains uncached, and no `_enumerations` table remains | `evaluate-unique-representation` |
| `bloat-presented-group-engine-registry` | **Closed.** Remove the presented-group-to-native-parent registry entirely rather than replacing its `MonoDict`: the only reader is quotient factorization, where the supplied target generator images already carry their Sage parent. **Acceptance:** `_native_group_engines` and `_native_engine` are absent; `GroupPresentation.factor()` derives one common native target parent from the validated image data and hands that parent directly to Sage/GAP's homomorphism constructor | `evaluate-unique-representation` |
| `bloat-indexed-module-data-registry` | **Closed.** Remove the indexed-integer-module metadata registry entirely: both the module category and additive carrier are recoverable from the module's existing public owner/forgetful structure, and indexed-freeness is already retained by the additive carrier owner. **Acceptance:** `_indexed_integer_module_data` and its record class are absent; indexed element/support/coefficient/map operations derive `Modules(ZZ, Ab)` plus the forgetful carrier and validate that carrier with `_indexed_free_record` | `evaluate-unique-representation` |
| `bloat-cokernel-difference-registry` | **Closed.** Remove the reverse projection-to-native-difference registry: the owned coequalizer presentation already retains the defining parallel pair, so its native difference can be reconstructed exactly when CAP computes the universal colift. **Acceptance:** `_cokernel_differences` is absent; public coequalizer mediation supplies the retained `f,g` pair to the engine, and `ModulePresentationsForCAP` recomputes `f-g` from their retained native morphisms before `CokernelColiftWithGivenCokernelObject` | `evaluate-unique-representation` |
| `bloat-relative-identity-coequalizer-registry` | **Closed.** Delete the relative-tensor identity-coequalizer marker instead of changing its container. The strict unit case is not an additive cokernel presentation at all: `relative_tensor` returns the literal identity, and the relative-tensor owner can factor through it directly. **Acceptance:** `_identity_coequalizers` is absent; the strict-unit branch stores no provenance, `_factor_relative_projection` returns the supplied arrow only for the cached identity projection, and every nonidentity relative quotient still delegates to the actual retained additive coequalizer | `evaluate-unique-representation` |
| `bloat-catlab-functor-recipes` | **Closed.** Delete Catlab's parallel functor-recipe registry rather than improving its cache: identity and composite provenance already belong to the owned category theory as `Identity()` placement and retained composite factors. **Acceptance:** `_functor_recipes`, `_retain_functor_recipe`, `identity_functor`, and `retain_composite_functor` are absent; lazy Catlab materialization reads public identity placement/composite factors directly and only falls back to the functor's declared callable actions for primitive functors | `evaluate-unique-representation` |
| `bloat-catlab-callable-transformation-recipe` | **Closed.** Remove the primitive callable-transformation recipe entirely rather than retaining the same source/target/component data twice. `NaturalTransformationData` already owns those three fields, and lazy Catlab materialization already falls back to them when no specialized native structural recipe exists. **Acceptance:** `callable_transformation` is absent from the engine API, `_construct_transformation` writes no Catlab recipe, primitive transformations materialize from their retained data, and specialized identity/composition/whiskering/horizontal recipes remain available for Catlab-native structural execution | `evaluate-unique-representation` |
| `bloat-canonical-shape-cache` | **Closed.** Replace `CategoryOfCategories`' hand-written canonical-shape dictionary and check/store protocol with Sage method caching keyed only by the mathematical shape presentation. **Acceptance:** `_canonical_shape` is a `cached_method` keyed by shape name and normalized integer parameters, `_canonical` state is absent, and each canonical constructor only supplies its admissibility checks plus native constructor | `type-paydown` |
| `bloat-exponential-action-cache` | **Closed.** Replace the hand-written `TripleDict` cache for the exponential action on Cat morphisms with Sage method caching. **Acceptance:** `exponential_on_morphism(exponent, functor)` is identity-keyed by its two mathematical inputs, `_exponential_actions` state is absent, and the cached functor retains the same postcomposition and left-whiskering actions | `type-paydown` |
| `bloat-fiber-construction-cache` | **Closed.** Replace the nested module-level `MonoDict` cache for strict fibers with Sage construction caching keyed by the defining functor and base object identities. **Acceptance:** `fiber(defining_functor, base_object)` is a `cached_function(key=identity_key)`, `_fibers` state is absent, and first construction still realizes the pullback plus retained inclusion before caching the result | `type-paydown` |
| `bloat-discrete-functor-cache` | **Closed.** Replace the two module-level identity registries behind the `Discrete: Sets -> Cat` object and morphism actions with Sage cached functions. **Acceptance:** `_discrete_on_object` and `_discrete_on_morphism` are `cached_function(key=identity_key)`, `_discrete_categories`/`_discrete_functors` are absent, and repeated public applications retain the same exact discrete category and functor | `type-paydown` |
| `bloat-query-comparison-cache` | **Closed.** Replace the string-keyed module dictionary retaining generated query-comparison predicates with Python's standard function cache. **Acceptance:** `_query_comparison(operation)` is `functools.cache`-owned, `_query_comparisons` is absent, and each comparison spelling still installs its exact handler once before reuse | `type-paydown` |
| `bloat-functor-construction-cache` | **Closed.** Replace `CategoryOfCategories`' nested `TripleDict`/`MonoDict` cache for functor construction with Sage method caching over the four exact defining identities. **Acceptance:** `construct_morphism(domain, codomain, on_object, on_morphism)` is identity-keyed by those four inputs, `_declared_functors` is absent, and repeated construction returns the same functor without invoking equality on owned categories or actions | `type-paydown` |
| `bloat-indexed-category-cache` | **Closed.** Replace the module-level `MonoDict` retaining `IndexedCategories(base)` with Sage function caching keyed by the exact base category. **Acceptance:** `IndexedCategories` is `cached_function(key=identity_key)`, `_indexed_categories` is absent, and repeated construction retains the same indexed-category owner without invoking proposition-valued category equality | `type-paydown` |
| `bloat-discopy-word-method-cache` | **Closed.** Remove the DisCoPy semantic model's hand-written word-result dictionary now that its identity key is explicit. **Acceptance:** `NonstrictMonoidalModel._word` is `cached_method` keyed by `identity_key(*word)`, `_word_cache` is absent, and repeated exact wire words retain one semantic object while still folding tensor products only on first evaluation | `type-paydown` |
| `bloat-fibration-lift-caches` | **Closed.** Keep the retained cartesian/cocartesian lift rules but replace their global `TripleDict` result caches with per-functor Sage method caches. **Acceptance:** `cartesian_lift` and `cocartesian_lift` are identity-keyed cached methods on the functor, `_cartesian_lifts`/`_cocartesian_lifts` are absent, and each registered lift rule executes once per exact morphism/object pair | `type-paydown` |
| `bloat-discopy-wire-method-cache` | **Closed.** Keep the reverse token-to-object table required by DisCoPy evaluation, but remove the parallel object-to-token `MonoDict` and its lookup protocol. **Acceptance:** `_wire_token(value)` is an identity-keyed cached method that allocates the token and reverse entry exactly once, `_wire_tokens` is absent, and `wire()` is only the public `Ty` wrapper | `type-paydown` |
| `bloat-narrowing-cache` | **Closed.** Replace each category's hand-written dictionary of canonical narrowings with Sage method caching on the exact closed root family. **Acceptance:** `intersection()` still returns an already-declared root when its closed set matches, otherwise delegates to identity-keyed `_narrowing(selected)`; `_narrowings` state is absent and distinct root families retain distinct canonical category identities | `type-paydown` |
| `bloat-identity-morphism-cache` | **Closed.** Replace each category's `_identities` `MonoDict` and manual first-construction protocol with an identity-keyed cached identity constructor. **Acceptance:** `_identity_morphism_(X)` is a `cached_method` keyed by `identity_key(X)`, first construction still retains the native cell, self-inverse pair, and `Identity()` placement, composition checks cache presence without constructing identities, and `_identities` state is absent | `type-paydown` |
| `bloat-finite-category-retention-cache` | **Closed.** Keep positive finite-category evaluation cached by exact owned-category identity, but remove the evaluator's parallel module-level `MonoDict` cache lifecycle. **Acceptance:** `_retained_finite_category` is a Sage `cached_function` keyed by `identity_key(category)`, `finite_category()` uses cache presence/set operations so `Unknown` remains deliberately uncached, and the evaluator owns no `_retained` registry | `type-paydown` |
| `bloat-narrowing-registration-cache` | **Closed.** Finish the canonical-narrowing cache migration at the external registration path instead of leaving `retain_intersection()` pointed at the deleted `_narrowings` table. **Acceptance:** externally supplied intersections normalize their root family with `closed_roots`, query/install the identity-keyed `_narrowing` Sage method cache through `is_in_cache`/`set_cache`, and `cat/category.py` contains no `_narrowings` reference | `bloat-narrowing-cache` |
| `bloat-axiom-application-registry` | **Closed.** Remove the parallel generated-axiom registry by retaining each declaring axiom on the generated application function itself. **Acceptance:** `application_axiom(owner, name)` reads the exact declaration namespace and function state, `_derived_applications` is absent, and semantic-collision checks still identify applications generated from the same axiom | `type-paydown` |
| `bloat-predicate-atom-type-cache` | **Closed.** Replace the hand-written runtime-type-to-SymPy-atom-class dictionary with Python's function cache. **Acceptance:** `_atom_type(domain)` is `functools.cache`-owned, `_atom_types` is absent, and semantic-base recursion plus refined owned-value atom reconstruction remain unchanged | `type-paydown` |
| `bloat-cw-open-datum-regression` | **Closed.** Repair the centralized CW-open datum boundary so finite and weak opens each unwrap through their typed owner rather than recursing or re-casting through the wrong helper. **Acceptance:** `_cw_open_datum` unwraps one finite-stage `CWOpen`, `_weak_cw_open_datum` unwraps one `_WeakCWOpen`, projective-infinity inverse-image maps use the weak helper, and no self-recursive datum accessor remains | `bloat-cw-open-datum-boundary` |
| `bloat-shared-identity-positions` | **Closed.** Consolidate the identical Sage-identity position-map helper used by FinSetsForCAP and finite category-limit lowering. **Acceptance:** `kernel.retention.identity_positions()` is the sole helper constructing `MonoDict` position maps, both engine adapters use it directly, and neither adapter carries a duplicate `_identity_positions` implementation | `type-paydown` |
| `bloat-limit-basis-shared-position-helper` | **Closed.** Route finite diagram-presentation vertex indexing through the shared identity-position primitive instead of retaining a third local `MonoDict` construction loop. **Acceptance:** `cat/limit_basis.py` imports `identity_positions`, contains no local `MonoDict` position builder, and its source/target incidence maps still index the exact retained vertices | `bloat-shared-identity-positions` |
| `bloat-cell-owner-registry` | **Closed.** Delete the homotopy-cell adapter's first-owner registry rather than replacing its `MonoDict`: the exact native signature owner is already determined by the supplied category's public `construction_owner()` after peeling the `Mor` tower. **Acceptance:** `_cell_owners` and `_cell_owner` are absent; every cell operation normalizes its supplied owner through `_root_owner`, `_root_owner` ends at `construction_owner()`, and later refinement cannot create a second native signature for the same construction owner | `evaluate-unique-representation` |
| `bloat-represented-product-shared-position-helper` | **Closed.** Route represented-product projection indexing through the shared identity-position primitive instead of keeping another local `MonoDict` enumerate/store loop. **Acceptance:** `SetsCategory._represented_product()` uses `identity_positions(vertices)`, `sets/finite.py` no longer imports `MonoDict`, and represented product projections retain the same exact vertex positions | `bloat-represented-product-position-map`, `bloat-shared-identity-positions` |
| `bloat-cartesian-comparison-dispatch` | **Closed.** Replace the hand-written category-type-to-Cartesian-comparison handler registry with Python's standard single-dispatch mechanism. **Acceptance:** `_native_cartesian_comparison` is `functools.singledispatch`-owned, `register_cartesian_comparisons()` registers the leaf handler directly on that dispatcher, and `_cartesian_comparison_handlers` is absent | `type-paydown` |
| `bloat-additive-dead-point-map` | **Closed.** Delete the unused additive carrier-map wrapper now that all morphism forgetting crosses the retained public forgetful functor directly at its call sites. **Acceptance:** `_point_map` is absent from `algebra/abelian.py`, `_points` remains the sole carrier-object helper, and no source/test reference to `_point_map` remains | `type-paydown` |
| `bloat-catlab-dead-native-transformation-wrapper` | **Closed.** Delete the unused Catlab transformation lookup wrapper now that every native transformation consumer goes through `ensure_native_transformation()` or the retained realization API directly. **Acceptance:** `_native_transformation` is absent from `engines/catlab.py` and no source/test reference remains | `bloat-catlab-callable-transformation-recipe` |
| `bloat-dead-set-map-table` | **Closed.** Delete the unused finite-set morphism table property rather than materializing every domain value behind a private accessor nobody calls. **Acceptance:** `SetsCategory.MorphismType` has no `_table` property, finite/native map execution continues through `_action` and retained finite presentations, and no source/test `_table` reference remains | `type-paydown` |
| `bloat-dead-runtime-implementation-lookup` | **Closed.** Delete the unused compiler scan that searched every runtime table for a declaration's compiled class. **Acceptance:** `runtime_implementation_class` is absent from `kernel/compiler.py`, no source/test/docs reference remains, and active runtime lookup continues through exact runtime nodes and `runtime_declaration()` | `type-paydown` |
| `bloat-cartesian-exact-dispatch-regression` | **Closed.** Preserve the old exact concrete-category admission while using standard single-dispatch registration. **Acceptance:** a registered handler executes only when `type(base) is category_type`; an unregistered subclass falls through to `None` rather than inheriting a native engine it did not select | `bloat-cartesian-comparison-dispatch` |
| `bloat-inverse-image-selected-registry` | **Closed.** Delete the parallel inverse-image registry because the selected inverse image is already a chosen pullback; keep the genuinely necessary early reservation at that universal-construction owner instead. **Acceptance:** `properties.py` has no `_inverse_images`/`TripleDict`; `ApexCategory._reserve_chosen_object()` exposes the selected apex while its cone is assembled; `inverse_image()` reuses `Pullbacks().chosen_object(diagram)`; and recursive bootstrap no longer needs a second owner-specific registry | `type-paydown` |
| `bloat-predicate-identity-registry` | **Closed.** Delete the separate equality-predicate identity registry rather than retaining a second flag for semantics already installed in SymPy's dispatcher. **Acceptance:** `mark_identity_predicate()` installs the generic exact identity handler as the sole representation of reflexive identity, `_identity_predicates` is absent, and category-owned equality still answers `ask(x == x) is True` while more specific undecided handlers fall through by `MDNotImplementedError` | `type-paydown` |
| `bloat-image-compose-wrapper` | **Closed.** Delete the image category's pass-through `compose_morphisms()` override: image-specific work belongs in `composite()`, while the generic category owner supplies identity short-circuits and inverse retention around it. **Acceptance:** `ImageCategory` no longer overrides `compose_morphisms`; strict/full images still retain target composites through `composite()`; and the public image consumer observes both left and right identity composition as the exact retained morphism | `type-paydown` |
| `bloat-image-object-registry` | **Closed.** Delete the strict/full image category's parallel object-membership registry: literal object-image identity is already retained by the defining functor's `FunctorImageCache`, while the separate morphism registry remains necessary for identities/composites constructed by the image category itself. **Acceptance:** `ImageCategory` has no `_object_members` or `_retain_object`; strict/full object membership reads only the defining functor image cache; the before/after image-construction consumers retain membership; and owner-registered full images still use refinement rather than this cache | `type-paydown` |
| `bloat-point-empty-structure-override` | **Closed.** Delete `PointCategory.structure_functors()` because it states exactly the generic `Category` default and no point-specific structure. **Acceptance:** `PointCategory` inherits `Category.structure_functors` directly and the targeted Cat-core consumer pins that owner identity | `type-paydown` |
| `bloat-fixed-endpoint-narrowing-defaults` | **Closed.** Audit finding rejected after the current-HEAD bootstrap counterexample: fixed endpoints are a semantic narrowing base, not the generic full-subcategory default. `Mor(C)(A,B).P()` must construct through the fixed Hom owner so property-constructor data cannot be reinterpreted as a new endpoint pair. **Acceptance:** `FixedEndpointCategory.narrowing_base()` returns itself, `narrowing_roots()` is empty, and the Cat-core tower consumer pins that exact owner | `type-paydown` |
| `bloat-dead-compiler-unresolved-sentinel` | **Closed.** Delete the orphan `_UNRESOLVED` sentinel from `kernel/compiler.py`; no source, test, specification, reflection lookup, or generated projection consumes it. **Acceptance:** the definition and all references are absent, with no replacement state introduced | `type-paydown` |
| `bloat-property-cat-wrapper` | **Closed.** Delete the one-use `_categories()` helper in `cat/properties.py`: its only caller is already the lazy pullback-runtime boundary and can derive `Cat` directly from the retained `Fun = Mor(Cat)` owner. **Acceptance:** `_subcategory_pullback_runtime()` obtains `functors = _functors()` once and returns `functors.base_category()`; no separate category-wrapper helper remains | `type-paydown` |
| `bloat-presented-integer-rows-wrapper` | **Closed.** Delete `_integer_rows()` from the CAP presented-module adapter: it was a one-call alias for `_integer_matrix_rows(UnderlyingMatrix(native_object))` with no cache, conversion policy, callback role, or independent ownership. **Acceptance:** public engine reconstruction reads the native relation matrix once and passes it directly to `_integer_matrix_rows`; no replacement wrapper is introduced | `type-paydown` |
| `bloat-property-morphism-wrapper` | **Closed.** Delete the one-use `_morphisms()` helper in `cat/properties.py`: it carried no state or semantic choice beyond a lazy import used by one fixed-endpoint inhabitation method. **Acceptance:** `_chosen_inhabitation()` performs the cycle-safe local import at the actual consumer and delegates directly to `morphisms.hom_inhabitation`; no wrapper remains | `type-paydown` |
| `bloat-axiom-application-name-wrapper` | **Closed.** Delete the one-use `_application_name()` helper: the axiom itself owns the generated application spelling, and the helper carried no state, dispatch role, import-cycle boundary, or reuse. **Acceptance:** `Axiom.application_name()` derives `is_<snake_case>` directly from its retained declaration name and no parallel naming function remains | `type-paydown` |
| `bloat-catlab-transformation-recipe-reader` | **Closed.** Delete the one-use `_transformation_recipe()` accessor: the specialized Catlab recipe registry remains genuine retained structural provenance, but its sole materialization consumer can read that registry directly. **Acceptance:** `ensure_native_transformation()` performs the identity-safe optional lookup itself; `_retain_transformation_recipe()` remains the single writer boundary; and no second read API remains | `type-paydown` |
| `bloat-cat-symbolic-inverse-functor` | **Closed.** `Cat` no longer manufactures a public inverse functor with fake actions from bare `Isomorphisms()` placement. The Cat isomorphism property admits refinement only after `Cat().retain_inverses()` has retained the executable opposite functor; direct property construction without that inverse data is rejected before an invalid public isomorphism can escape. Generic data-free categories retain their symbolic inverse construction. **Acceptance:** `tests/kernel/test_cat_inverse_execution.sage` rejects both direct Cat-isomorphism construction and refinement without retained inverse data, then executes both actions of a retained inverse functor and preserves symbolic inversion for a discrete data-free category; the former `CategoryOfCategories._symbolic_inverse_` assertion-stub implementation is deleted | `type-paydown` |
| `kernel-cat-bootstrap-regression` | **Closed at its accepted revision.** The homotopy-cell adapter preserves supplied `Mor` level while sharing native state at the root signature; primitive Catlab transformation materialization executes the retained declaration assignment instead of recursively calling the public Catlab-backed `component()`; fixed-endpoint property construction retains its Hom category as narrowing base; and `Fun(I,-)` dispatches its category/functor overload by categorical role rather than incidental placement. **Historical acceptance evidence:** the fresh Sage 10.9 / Python 3.14 bootstrap printed `bootstrap-ok`, and the complete `test_functors_and_two_morphisms_are_owned_and_distinct` consumer executed with both left and right whiskers typechecked as native 2-cells and their exact functor boundaries retained. The exact historical runtime does not override the current supported runtime range | `type-paydown` |
| `current-head-acceptance` | **Closed at its recorded revision; not a certificate for later dirty work.** Primitive natural transformations retain their homotopy cells in the root signature while preserving the supplied `Mor` level through fixed-endpoint/subcategory construction owners. **Historical acceptance evidence:** `kernel-cat-bootstrap-regression` was closed and `tests/kernel/test_m2_cat_core.sage::test_functors_and_two_morphisms_are_owned_and_distinct` passed under the then-selected Sage 10.9 / Python 3.14 environment with primitive transformation, both whiskers, and horizontal composite typechecked as native 2-cells with exact retained functor boundaries | `acceptance`, `kernel-cat-bootstrap-regression` |
| `bloat-indexed-basis-membership` | **Closed.** Remove exception-driven membership probing from the Sage facade used by indexed free abelian groups. **Acceptance:** `_OwnedIndexFacade.__contains__` delegates to the owned index set public membership operation instead of calling `representative()` and translating assertion/type/value failures into ordinary control flow | `type-paydown` |
| `bloat-cell-generator-classification` | **Closed.** Replace exception-text probing of native homotopy generators with an explicit binding query. **Acceptance:** native `Cell.is_generator()` exposes whether a cell is a signature generator, and `_strengthen_generator_invertibility()` strengthens only those cells without catching or inspecting `ValueError` text; composite and inverse cells remain unchanged | `type-paydown` |
| `bloat-module-underlying-morphism-wrapper` | **Closed.** Delete the one-use module scalar-restriction wrapper for the underlying monoid morphism. **Acceptance:** `ModuleCategory.restriction()` binds the retained monoid-to-magma functor once and obtains the carrier morphism at the consumer; no private wrapper duplicates that two-step public forgetful path | `type-paydown` |
| `bloat-symbol-category-wrapper` | **Closed.** Delete the one-use `_symbol_category()` wrapper in named-operation construction. **Acceptance:** `_named_copy()` constructs the one-object symbol category directly as `Discrete(Sets((symbol,)))`; no separate helper restates that constructor without cache, conversion policy, or reuse | `type-paydown` |
| `bloat-catlab-retain-transformation-wrapper` | **Closed.** Delete Catlab's one-use retain-and-return transformation wrapper. **Acceptance:** `ensure_native_transformation()` retains the selected native transformation directly at the materialization boundary and returns it; no helper exists solely to call `retain_native_transformation()` and echo its input | `type-paydown` |
| `bloat-catlab-project-directory-wrapper` | **Closed.** Delete the one-use Catlab JuliaPkg project-directory wrapper. **Acceptance:** `_main()` derives its Catlab project path directly from the shared cache root before creating/exporting it; no helper exists solely to append the fixed `sage-categories/catlab-juliapkg` suffix | `type-paydown` |
| `bloat-additive-negation-wrapper` | **Closed.** Delete the one-use `_apply()` wrapper used only by additive group negation. **Acceptance:** additive point negation reads the retained carrier point and re-owns the image inline; the shared `_carrier_point()` remains the owner of generalized-element transport and no private helper exists for this single unary operation | `type-paydown` |
| `bloat-finite-representative-wrapper` | **Closed.** Delete the one-use finite-set representative-search wrapper. **Acceptance:** `SetsCategory.ObjectType.representative()` performs its equality-aware finite fallback at the owning public method after the exact hash lookup; no module helper exists solely to loop over one retained finite presentation | `type-paydown` |
| `bloat-certified-multiplicative-wrapper` | **Closed.** Delete the one-use certified multiplicative-monoid wrapper in commutative-ring reconstruction. **Acceptance:** `certified_commutative_ring()` directly renames the certified neutral monoid into `MultiplicativeMonoids(monoidal)`; the shared `_certified_monoid()` remains the operation/law boundary and no helper exists solely for one naming step | `type-paydown` |
| `bloat-owned-predicate-application-wrapper` | **Closed.** Delete the one-use `_owned_application()` predicate wrapper in proposition evaluation. **Acceptance:** the `ask()` fast path states its exact `AppliedPredicate`/owned-`Predicate` classification at the branch that consumes it; no helper exists solely to restate those two `isinstance` checks | `type-paydown` |
| `bloat-property-subcategory-wrapper` | **Closed.** Delete the one-use default-property-category wrapper in axiom construction. **Acceptance:** `Axiom._construct_declared()` reads `PropertySubcategory` directly through the existing lazy `_properties()` import boundary when no custom implementation is supplied; no second helper exists solely to return that class | `type-paydown` |
| `bloat-open-key-type-boundary` | **Closed.** Exact represented-open keys now remain parameterized through `TopologicalSpaces`, `RingPresheaf`/`RingSheaf`, and `RingedSpaces`; finite topology construction retains poset-owned open points and inverse images return through the public key/object boundary. Exact endpoint admission avoids evaluating already-matched functors, and canonical narrowing weakening is recognized directly instead of constructing the subset lattice needed only to expose identities during the geometry consumer. **Acceptance:** `TopologicalSpaces.from_open_category`, `RingPresheaf`/`RingSheaf`, and `RingedSpaces.homomorphism` retain one coherent key type across key-to-open/open-to-key conversion; finite `frozenset` opens, affine-open objects, projective-line labels, CW/adèlic opens, and the nonenumerated tuple-key regression keep their actual domains; generated stubs preserve those parameters; and finite plus nonenumerated public geometry consumers execute on Python 3.14 Sage without enumerating represented open categories | `type-paydown` |
| `bloat-indexed-module-public-construction` | **Closed.** The canonical tensor-unit action is recognized at `ModuleCategory.__call__` by monoidal coherence, so indexed free integer modules construct through the public module owner rather than through `ModuleCategory._algebras` or manual law-category refinement. **Acceptance:** the canonical left-unitor action on `indexed_free_abelian_coproduct(S)` for an infinite or nonenumerable owned index `S` constructs through a module-owned public operation with the same `Modules(ZZ, Ab)` object and carrier; `algebra/indexed_modules.py` contains no `_algebras` probe or manual refinement into `modules.ambient()`/`modules`; and `tests/algebra/test_indexed_free_modules.sage` proves arbitrary-index finite support, canonical injections, and the independent coproduct mediator without enumerating `S` | `module-sums`, `type-paydown` |
| `acceptance` | **Closed at its accepted revision; not a current-HEAD certificate.** Section 20 in full. The required public consumers are retained by the closed prerequisite nodes; displaced additive `Presentation`/`LinearForm` execution and the generic `Sets` map-form engine are deleted, presented Hom validation/equality and coequalizer arithmetic execute through ModulePresentationsForCAP, presented group words execute through Sage/GAP, and the Catlab/DisCoPy/native-cell integration paths remain the sole selected execution paths for their allocated operations. **Acceptance:** the native nonidentity coequalizer/mediator consumer and both exact-Hom consumers pass after the removal, `just generate-stubs` regenerates the reduced public surface cleanly, and the ownership boundary is recorded in `specs/resolution.md` | `host-headroom`, `monodict-autofix`, `runtime`, `sets`, `functors`, `paths`, `universal`, `indexed`, `diagrams`, `refinement`, `inverses`, `isofibrations`, `named`, `additive`, `tensor`, `relative`, `actions`, `kan`, `orders`, `groups`, `modules`, `module-sums`, `algebras`, `rings`, `spaces-sheaves`, `affine`, `gluing`, `topological-colimit`, `adeles`, `gate`, `python-runtime`, `static` |

</details>

## Maintenance outside the execution DAG

Code-quality review is not a standing execution node and must not keep the queue artificially open.
When an implementation change, failing public consumer, or concrete source reading exposes duplicated machinery, a dependency-owned algorithm, a shallow test, dead code, or an ownership defect, repair that defect at its existing owner and add the smallest behavioral regression that protects the result.
A broad finding becomes an ordinary DAG node only when it identifies a real unfinished capability with concrete acceptance.

The 2026-09-16 consolidation incorporates the coherent source changes left by the last audit pass and then returns to this rule.
Historical `bloat-audit-loop` references in `COMPLAINTS.md` identify where findings originated; they are not a permanent active queue after `6addad16`.

A review that finds no defect creates no task, complaint, status update, or commit.
Repository audit skills remain available as diagnostic lenses when a concrete risk or changed boundary warrants them, but periodic review is maintenance activity rather than a prerequisite for completion and never substitutes for delivering the mathematical or software behavior named by the active plan.

## Decision coverage at acceptance

This mapping connects the complete [architectural decisions](DECISIONS.md) to implementation and acceptance owners.
It is a review obligation, not a keyword-based certificate.
Each node's full topic contracts and every applicable declaration remain in scope even when an example is not named here.
Source paths in the active rows are relative to `src/sage_categories` unless otherwise specified, and unqualified Python filenames continue the preceding directory; named `.sage` consumers are in `tests/kernel`.

| Decision and controlling nuance | Implementation owner | Required acceptance |
| --- | --- | --- |
| [Functorial inheritance](DECISIONS.md#functorial-inheritance-and-leaf-simplicity): all eligible selected paths; selection asserts the repository's faithfulness condition, which arbitrary isofibrations do not imply; source execution realizes `x.f() = F(x).f()` through ordinary functor actions, with separate public images | `core-selected-transport`, `core-properties-refinement` | Objects, points and morphisms initialize in dependency order, once per shared runtime owner; multiple targets and distinct additive/multiplicative structures remain distinct, including on one carrier and in both construction orders. No second leaf transport description or lifecycle API. |
| [Existing higher cells](DECISIONS.md#existing-machinery-and-declaration-data): reuse the native `Mor` tower, homotopy-core and executable Cat/Catlab transformations | `core-functor-cell-calculus` | Correct globular source/target, dimensions, identities, composition, whiskering and inverses. Preserve directed ordinary arrows. Formal boundaries plus invertibility are distinct from executable components and inverses; component rules cover unenumerated domains. The ordinary strict 2-category fragment does not replace the broader higher semantics. |
| [Coherence and diagnostics](DECISIONS.md#coherence-and-compiler-diagnostics): actual cells between parallel retained functor composites justify interchange where required | `core-inheritance-coherence`, `core-static-and-boundaries` | The compiler uses generic/supplied comparisons and necessary argument/result transport, names the exact missing boundary, operation and compatibility, and prevents unjustified interchange. This deliberately extends the diagnostic-only policy; it does not reject distinct structures merely for sharing a target. |
| [Higher invertibility and audit](DECISIONS.md#higher-invertibility-and-mathematical-audit): trusted declarations and mathematical truth have different proof burdens | `core-inheritance-coherence`; all B families and `completion-current-head-acceptance` | Ambient higher invertibility supplies inverses to existing cells, not missing fillers or arbitrary parallel comparisons. Generic structural coherence is derived centrally; leaves supply genuinely additional mathematical data without an infinite witness tower. The compiler's structural acceptance and each leaf's component/inverse/compatibility audit are evidenced separately. |
| [Named categories and concrete structure](DECISIONS.md#generic-constructions-and-named-categories): `Sets()` exists before implementation and is strengthened through its identity functor | `core-owned-interfaces`, `core-universal-calculus`, `leaves-sets-order` | Earlier values/maps retain the identical mathematical owner; the generic faithful route supplies `functor_to_sets()`, `underlying_set()` and `underlying_map()`. Not finding a selected route is not a proof of nonconcreteness. Creation/preservation/lifting hypotheses, such as `CreatesLimits(I)`, govern reconstruction; faithfulness alone does not. |
| [Universal constructions](DECISIONS.md#generic-constructions-and-named-categories): Cat owns limits/colimits and their reductions; leaves supply diagrams and genuinely additional chosen lifts | `core-universal-calculus`, the three structured-calculus prerequisites, `leaves-geometry` | Retain presentations, oriented maps and mediators, not just apices. Ordinary product/equalizer and coproduct/coequalizer reductions respect existence hypotheses; higher versions retain coherent data. Compatible-section rings, induced stalk maps and ringed-space composition run through these same generic owners. |
| [Typed ownership](DECISIONS.md#mathematical-ownership-and-typed-interfaces): actual categories, functors, presentations, projections and cells are the semantic interface | `core-owned-interfaces`, `core-static-and-boundaries`; every B family | Split assembly responsibilities between mathematical choices, kernel identity/retention, private native values, functor images and actual comparisons. Private dictionaries remain possible; semantic string-keyed assembly and `Any` escape hatches do not. Runtime identity, equality and equivalence remain distinct. |
| [Engine delegation](DECISIONS.md#engine-delegation-and-research-precedents): use existing native computations and mature derivations in their declared domains | Every behavioral node, checked again by A and B | Sage owns C3/runtime machinery; SymPy owns proposition algebra; CAP's weighted multi-prerequisite closure supplies applicable derived computations, not categorical path coherence. GATlab serves suitable private fragments without a second public model/theory semantics. MathComp/Hierarchy Builder are research precedents, not mandated dependencies or replacement hierarchy assumptions. New infrastructure requires a verified gap in existing dependencies and a second real consumer. |
| [Computational generality](specs/computational-generality.md): engine restrictions do not narrow the mathematical universe | Every behavioral node, `leaves-infinitary-consumers`, A and B | Preserve infinite/nonenumerable carriers, presentations and indexing as specified; executable symbolic/component rules need not enumerate them. A finite prefix, strict/identity example or raw engine-only consumer cannot stand for the required public domain. |
| [Architectural enforcement](DECISIONS.md#architectural-enforcement): positive architectural roles and declaration-derived invariants cover future code | `core-static-and-boundaries`; all B families and `completion-current-head-acceptance` | Exhaustive module classification, protected internals and indirect dependency checks; exact types/exports; compiler endpoint, initialized-state, selected-structure and required-comparison validation; automatically applicable category/functor/transport/universal/reconstruction contracts. A exercises the mechanisms and first consumers; B establishes every production module's conformance. Generic/stateful testing may expose new sequences; no scanner proves arbitrary Python mathematics. |
| [Leaf simplicity and isolation](DECISIONS.md#functorial-inheritance-and-leaf-simplicity) and [dependency directions](specs/system.md#dependency-directions) | All B families, `leaves-complete` | All production leaves/helpers contain their local mathematics and private backend wiring. Legitimate immediate mathematical targets remain importable. Kernel and Cat do not import leaves; `cat_kernel` supplies joint interpretation through bootstrap. A generic improvement reaches leaf consumers without coordinated plumbing edits or a hidden generic engine in a firewall. |

## Use the graph at the consumer boundary

The active milestone A and B tables are the work selector.
Their top-to-bottom order is the tie-breaker among ready nodes; `Needs` records only real data/behavior dependencies.
Do not manufacture dependency edges merely to force scheduling.

Complete the shared foundation through its real first consumers before full leaf convergence.
Finish the selected node through its complete public consumer before moving to another family.
Preserve accepted runtime, engine and static capabilities while repairing ownership; a foundational defect belongs to its A owner even when a B consumer exposes it.

A node that adds or changes public mathematics carries its static projection in the same unit.
`core-static-and-boundaries` verifies the shared integrated projection; `completion-current-head-acceptance` verifies the complete leaf projection and runtime together.
The retained `completion-static-integration` row supplies earlier evidence, not present acceptance.
Neither integration node postpones a feature's first behavioral consumer or per-feature typing.

The source-built Sage runtime is an external prerequisite only for execution acceptance.
While it is building, continue source work whose correctness can be established by source/specification reasoning without running Sage.
Do not create a second Sage runtime or weaken a consumer to make progress.

`publish-for-consumers` retains its independently authorized historical publication.
Any subsequent publication follows the repository-owner policy in `README.md`. The terminal `framework-complete` node requires the revision satisfying both A and B to be reachable from `origin/main`; publishing an intermediate snapshot establishes neither milestone.

After changing this graph, run `just plan-state`. Its local validator checks unique IDs, resolved prerequisites, closed prerequisites of closed nodes, acyclicity, historical/active separation, and a path from every active node through its milestone to `framework-complete`. Every B node must depend transitively on `kernel-cat-complete`; A's first consumers must not depend on B's closure.
The A/B headings delimit active scope, including closed active nodes; rows outside that scope must be closed historical evidence and cannot depend on current work.
Reopening an old obligation requires routing it into the appropriate active milestone.
Preserve the earlier IDs and obligations when moving historical rows.
Graph validation does not prove semantic coverage: compare the [decision-to-consumer mapping](#decision-coverage-at-acceptance) with the full decisions and topic contracts whenever obligations change.
Keep implementation evidence in source, consumers, issues, and commits; this table is routing, not a second proof ledger.

[plan]: .agents/plans/features/FEATURE-functor-owned-category-framework/plans/PLAN-native-engine-remediation/PLAN-native-engine-remediation.md
