# Exact derived-category implementation: governing-plan locator

Section 4 of `PLAN-native-engine-remediation` now owns the complete repair.
Retrieve the plan through [the remediation continuation](remediation-handoff.md).
The former handoff is superseded, not an independent work order.

Preserve `tests/kernel/test_exact_derived_category_implementation.sage` and the
exact retained `AdditiveGroups(Cartesian(Sets())).Commutative()` owner, including
values and functor images constructed before installation. The governing plan
specifies the class-joining repair, native installation boundary, and additive
consumer; the public specimen is not replaced by a new computational category.

The full historical implementation declaration, source locators, preservation
checklist, and cutoff evidence remain available at:

```bash
git show b1fde1f6f65deaf9b405c8d7e0add60a8011fd1d:docs/remediation-derived-handoff.md
```
