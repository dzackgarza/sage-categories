# Exact derived-category implementation: continuation locator

`core-owned-interfaces` in [TODO.md](../TODO.md) owns exact-category installation; `core-algebraic-calculus` and `leaves-algebra` own its additive/tensor consumers.
Retrieve historical allocation through [the framework continuation](remediation-handoff.md).
The former handoff is superseded, not an independent work order.

Preserve `tests/kernel/test_exact_derived_category_implementation.sage` and the exact retained `AdditiveGroups(Cartesian(Sets())).Commutative()` owner, including values and functor images constructed before installation.
Extend the existing class-joining and native installation owners only for demonstrated failures; the public specimen is not replaced by a new computational category.

The full historical implementation declaration, source locators, preservation checklist, and cutoff evidence remain available at:

```bash
git show b1fde1f6f65deaf9b405c8d7e0add60a8011fd1d:docs/remediation-derived-handoff.md
```
