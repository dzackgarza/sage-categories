# Framework continuation

Select current work from the active A/B tables in [TODO.md](../TODO.md).
Their prerequisites, existing implementation owners and complete consumers converge on the complete kernel/Cat foundation and then all production leaves.
[DECISIONS.md](../DECISIONS.md) supplies the architectural direction; [the system ownership map](../specs/system.md#ownership-map) supplies full contracts.

The project-vault `PLAN-native-engine-remediation` is archived.
Retrieve its native-engine allocation and preservation obligations when needed by the selected node:

```bash
agent-memory plan show PLAN-native-engine-remediation
```

Its retained sections 19 and 20 delegate later execution to TODO and preserve the completed native slice.
Earlier numbered sections are historical material, not current routing.
Exact-category installation belongs to `core-owned-interfaces`; additive/tensor integration to `core-algebraic-calculus` and `leaves-algebra`; static projection accompanies every behavioral node and is integrated by `core-static-and-boundaries` and `completion-current-head-acceptance`. Retain existing public issue contracts/dependencies; their native-slice completion does not establish the broader A/B acceptance.

The earlier core, scaffold, static, and remediation plans and their phases have been removed from the execution graph and preserved as historical references.
Existing public mathematical consumers and exact-revision evidence remain intact; historical completion labels do not establish current milestone acceptance.

## Historical planning records

The former card bodies and metadata, including accepted revisions, are retained as references rather than runnable plans:

```bash
agent-memory retrieve projects/github.com__dzackgarza__sage-categories/references/superseded-core-and-foundation-planning-records-2026-09-07
agent-memory retrieve projects/github.com__dzackgarza__sage-categories/references/superseded-leaf-scaffold-planning-records-2026-09-07
agent-memory retrieve projects/github.com__dzackgarza__sage-categories/references/superseded-static-projection-planning-records-2026-09-07
```

## Historical checkpoint evidence

The former handoff's source checkpoints, evidence table, full consumer examples, commands, and remote-recovery record remain available without becoming another current plan:

```bash
git show b1fde1f6f65deaf9b405c8d7e0add60a8011fd1d:docs/remediation-handoff.md
```

[The derived-category locator](remediation-derived-handoff.md) and [the static-projection locator](remediation-static-handoff.md) preserve access to their detailed historical specimens and experiments.
For current routing health, run `just plan-state`; for implementation acceptance, follow the selected node's public consumer.
Historical failure records remain evidence at their recorded revisions, not assertions of a current failure or permission to bypass a gate.
