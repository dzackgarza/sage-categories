[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "sage-categories-homotopy"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "sage_categories_homotopy"
features = ["pyo3/extension-module"]
